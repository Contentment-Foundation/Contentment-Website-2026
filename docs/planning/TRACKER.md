# Project Tracker — contentment.org

> **Last updated:** 4 August 2026 · Somesh Bhardwaj  
> **Spec detail** (acceptance criteria): [FEATURE-TICKETS.md](./FEATURE-TICKETS.md) · **Decisions**: [DECISIONS.md](./DECISIONS.md)

---

## Status key

| Symbol | Status | Meaning |
|--------|--------|---------|
| 🔵 | Open | Ready to start, no blockers |
| 🟡 | In Progress | Actively being worked on |
| 🟠 | Blocked | Waiting on external input or dependency |
| ⏸️ | Pending | Waiting on a team decision |
| ✅ | Done | Completed and verified |
| 📅 | Scheduled | Planned for a later phase |
| 🚫 | Cancelled | Dropped — will not do |

**Prefixes:** `FEAT` = feature · `OPS` = infrastructure/config · `DOC` = documentation · `QA` = quality/audit

---

## Master Ticket Table

| ID | Title | Cat | Phase | Priority | Status | Raised by | Owner | Opened | Closed | Depends on | Blocker / Note |
|----|-------|-----|-------|----------|--------|-----------|-------|--------|--------|------------|----------------|
| FEAT-001 | Extract shared layout (CSS tokens, nav, footer) | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | — | `src/layouts/BaseLayout.astro` + `Nav.astro`/`Footer.astro` + tokens/global.css from Dave Jul 29 handoff. Mobile drawer shipped (FEAT-003 ✅ 30 Jul). **Aug 4 (Somesh)** — Nav current-page underline (`aria-current=page`; white on transparent header, `--btnblue` when scrolled). |
| FEAT-002 | Multi-page routing scaffold (Astro) | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-001 | Astro 4.x confirmed (D-23/HC-055). All 7 Phase 1 content routes under `src/pages/`. Netlify preview publishes `dist/` (cut over 30 Jul → contentmentweb2.netlify.app). **2 Aug — branded `/404` shipped** (`src/pages/404.astro`; `noindex, follow`, reuses existing tokens + `.anim`, 6 destination cards, no new design language). **Astro switched `static` → `hybrid` + `@astrojs/netlify@5.x` adapter (2 Aug), then reverted to `output: 'static'` with no adapter (3 Aug)** — no route ever set `prerender = false`, so hybrid deployed a live SSR function, edge middleware and `/_image` endpoint carrying 5 open Dependabot alerts for zero shipped benefit. Static `dist/` is file-for-file identical minus `_redirects` (netlify.toml already covers those 3 routes as 200 rewrites). Re-blocks FEAT-070's hosting until the Astro 4→7 migration. `/privacy` + `/terms` still blocked on legal copy (FEAT-071). |
| FEAT-003 | Mobile navigation drawer | FEAT | 1 | Must | ✅ Done | Somesh | Engineering | Jun 2026 | 30 Jul 2026 | FEAT-001 | Slide-in drawer in `Nav.astro` + `nav.js`: focus trap, Escape, body scroll lock, Join Homeroom + Donate CTAs. See `HOMEPAGE-RESPONSIVE-AUDIT.md`. **Aug 4 (Somesh)** — drawer links also use `aria-current=page` + white underline for the active route. |
| FEAT-004 | Wire all nav and footer links | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-002 | **Aug 4 —** Get Involved slug `/getinvolved` (was `/give`); Donate → `/getinvolved#become`; `seams.join=/getinvolved`. Join Homeroom tiers → `/?amount=N&frequency=monthly#homeroom`. **Aug 3 Nav:** Events RSVP → `#ev-signup`; school mailto; partner deck live. Dead: `waysToGive` `#`, Spread the movement `#`, Flodesk stubs. HC-005 Open. |
| FEAT-010 | Migrate homepage to `/` route | FEAT | 1 | Must | ✅ Done | Somesh | Dave Kebo | Jun 2026 | 31 Jul 2026 | FEAT-001, FEAT-002 | Jul 29 — `src/pages/index.astro` + 10 section components. Responsive audit done 30 Jul. **31 Jul — Slack review passed (HC-057).** **Aug 3 —** CTA wires (hero/invite/doors); Homeroom form measure 46ch; Priscillah card remains Dave designed graphic. **Aug 4 —** Keela General Donation Form live in Homeroom band (FEAT-060 interim). Per-tier Homeroom join still HC-075. **Aug 4 (RF-010 Dave)** — hero zoom matched Get Involved (global `.hero-bg` scale 1.06 / 18s). |
| FEAT-011 | Homepage copy audit vs messaging brief | FEAT | 1 | Should | ✅ Done | Somesh | Engineering | Jun 2026 | 31 Jul 2026 | FEAT-010 | 31 Jul — Four Pillars verbatim; Homeroom CTA. **Aug 3 —** D-01: InviteBand + homepage meta `$5` → `$25/month` (HC-041). **Aug 4 —** dummy donate form (and `data-group="donor"`) replaced by Keela General Donation Form. |
| FEAT-020 | Build `/why` page | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-001, FEAT-002 | Jul 29 — ported to `src/pages/why.astro`. 31 Jul RF-006 QA pass. 1 Aug — video lightbox sizing fixed (RF-008 pattern). **Aug 3 —** teacher reels fill 9:16; donate form measure 52ch. **Aug 4 —** Keela General Donation Form live in give band. Newsletter still placeholder. **Aug 4 (RF-010 Dave)** — hero zoom matched Get Involved (global `.hero-bg` scale 1.06 / 18s). |
| FEAT-030 | Stories data model + JSON seed (min 3 stories) | FEAT | 1 | Must | 🟠 Blocked | Somesh | Engineering | Jun 2026 | — | — | Waiting on comms for story content + photos. Index UI can ship without this; individual `/stories/[slug]` cannot. |
| FEAT-031 | Build Our Impact index (`/our-impact`) | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-002 | Jul 29 — ported to `src/pages/our-impact/index.astro` from Dave Our Impact handoff. Route is `/our-impact` (not `/stories`). 31 Jul RF-006 QA pass (320–1280 clean; no code fixes). Story data model (FEAT-030) still blocked. **1 Aug — RF-008 (Kristina): video lightbox playing in a tiny frame — fixed.** Root cause: the modal `<iframe>` is created dynamically via JS `innerHTML`, so it never receives Astro's scoped-style attribute, and the CSS meant to size it to the modal (`width/height:100%`) silently never matched — iframe fell back to the browser's ~300×150 default. Fixed by setting size inline on the iframe string itself (same fix applied to `/why`, which had the identical bug). |
| FEAT-032 | Build `/stories/[slug]` individual template | FEAT | 1.5 | Should | 📅 Scheduled | Somesh | Engineering | Jun 2026 | — | FEAT-030, FEAT-031 | — |
| FEAT-033 | Interactive global educator map | FEAT | 2 | Nice | 📅 Scheduled | Somesh | Engineering | Jun 2026 | — | FEAT-031 | — |
| FEAT-040 | Build `/schools` page | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-002 | Jul 29 — ported to `src/pages/schools.astro` from Dave For Schools `fs_review1` handoff (verbatim; some images still base64). 31 Jul RF-006 QA pass (320–1280 clean; matrix side-scroll / fsr-bloom intentional). **Aug 3 —** partner deck Drive URL wired; Google Form embed deferred Phase 2 (Kristina Miro); pricing `.col-name` bumped to 1.4rem. **Aug 4 (Somesh)** — wellbeing-lead photo replaced at `public/assets/fs-Jadielsm.jpg` (same filename / src). **Aug 4 (RF-010 Dave)** — hero zoom matched Get Involved (global `.hero-bg` scale 1.06 / 18s). |
| FEAT-041 | School discovery form | FEAT | 2 | Should | 📅 Scheduled | Somesh | Engineering | Jun 2026 | — | FEAT-040 | **31 Jul** — Google Form + Slack built and was embedded on `/schools`. **Aug 3 Kristina Miro:** hold embed for Phase 2 — "test the simple page first and then integrate it if we see that people are not using the form." Embed commented out; `discoveryFormUrl` cleared (URL in seams comment). Launch path = `#start` mailto / Start a Conversation. |
| FEAT-050 | Build `/getinvolved` gateway page | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-002 | Jul 29 — Get Involved ported. **Aug 4 —** route `/getinvolved` (was `/give`; 301 kept). 31 Jul RF-006 QA. **Aug 3 —** hero Join Homeroom → `#become`. D-03 still open. Join Homeroom tiers → `seams.homeroomDonateUrl`; `joinTiers` empty. **Aug 4 (Somesh)** — donate split `#donate` (photo `gi-donate-photo.jpg` + Keela General Donation Form, same embed as homepage); Prefer to give differently one-time → `#donate`. **Aug 4 (RF-010 Dave)** — hero zoom is the reference model for Home/Why/Schools; Meet Jose / Homeroom video cut HELD pending WoeiJing + Kristina. |
| FEAT-051 | Build `/give/monthly` conversion page | FEAT | 1 | Must | 🟠 Blocked | Somesh | Engineering | Jun 2026 | — | FEAT-050, FEAT-060 | Homeroom monthly UI currently lives at `/getinvolved`. Separate monthly route not created; blocked on per-tier Homeroom Keela (HC-075 remainder) + D-03 routing. General Donation Form does not close this. |
| FEAT-060 | Keela donation integration — wire all CTAs | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | — | **Aug 4 —** General Donation Form live (org `CBbknhqovLi8DNEzW`, embed `give-usa…/MnqZFksL49Ym3M8Ho`) via `KeelaScripts` + `KeelaDonateForm`; used on `/` Homeroom + `/why` give band. Donate CTAs → `/#homeroom`. **Aug 4 follow-on —** `/getinvolved` Join Homeroom `$25/$50/$100` → `/?amount=N&frequency=monthly#homeroom` (`seams.homeroomDonateUrl`); iframe gets amount+frequency params. **Aug 4 (Somesh)** — same Keela embed also on `/getinvolved#donate` split. India + per-tier `joinTiers` still deferred (**HC-075** remainder). |
| FEAT-070 | Newsletter integration (Flodesk embed or API) | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-010, credentials | **4 Aug — SHIPPED, and unblocked WITHOUT the adapter.** The hosting blocker was never really about Astro: §6.2 only needs a server-side place to hold `FLODESK_API_KEY`, and a **host-native function** provides that. `netlify/functions/newsletter.mjs` (preview) + `api/newsletter.js` (Vercel target) share one core, `src/lib/flodesk.js`; `netlify.toml` rewrites `/api/newsletter` → the function so both hosts expose the same public path. `astro.config.mjs` stays `output: 'static'` with no adapter — the 3 Aug security revert is untouched and **FEAT-101 is no longer a blocker for this ticket**. Flodesk contract verified against developers.flodesk.com first (Basic auth `base64('KEY:')`; `POST /v1/subscribers` then `POST /v1/subscribers/{email}/segments`) — the 31 Jul revert was caused by an unverified contract, so that was checked before any code. **11 capture points** across home, `/about`, `/why`, `/updates`, `/events`; honeypot + inline `aria-live` success/error; the 4 Events link-CTAs share one `<CaptureModal />` that reads copy + segment off `data-capture-*`. Segment per form resolves through `SEGMENT_ENV_BY_SOURCE` → env vars (IDs never hardcoded; `scripts/flodesk-segments.mjs` lists them). Assignments came from **Kristina's Miro CTA suggestions**: 8 → `www.contentment.org`, hero Save-my-free-spot → `Contentment Festival`. **Still pending Kristina + WoeiJing (D-24):** `/updates` form, `/events` top capture fold, and the 3 Upcoming-grid CTAs (Save my spot / Be first to know / Join the waitlist), which still only scroll to `#ev-signup`. Interim `FLODESK_SEGMENT_DEFAULT` → `www.contentment.org`. **Not yet done:** live end-to-end submit test, rate limiting (`UPSTASH_*` unset), Vercel-side verification at cutover. Note: `newsletter_submit` now fires on a **confirmed subscribe**, not on click (TICKET-080) — the number will drop and become real. |
| FEAT-071 | `/privacy` and `/terms` pages | FEAT | 1 | Must | 🔵 Open | Somesh | Engineering | Jun 2026 | — | FEAT-002, legal copy | Waiting on legal copy; cookie approach resolved (DECISION-002, Somesh) |
| FEAT-080 | Analytics setup (GA4 + PostHog Cloud + Clarity + Osano + Sentry) | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-002 | 31 Jul — scaffolded: `src/components/Analytics.astro` (Osano → GA4 Consent Mode v2 → Clarity → PostHog, each no-op until its own env var is set), Sentry via `@sentry/astro` (client-side only, registered only when `SENTRY_DSN` is set), `cta_homeroom_click`/`newsletter_submit` wired. `.env.example` + `PUBLIC_CLARITY_ID` doc gap added. Blocked on real credentials — Somesh sourcing GA4/PostHog/Osano/Clarity/Sentry IDs (**HC-076**). `npm run build` verified both with all vars unset (today's state, clean) and with vars set (scripts render correctly). |
| FEAT-081 | SEO baseline (meta, OG, sitemap, robots, `llms.txt`, favicon) | FEAT | 1 | Should | ✅ Done | Somesh | Engineering | Jun 2026 | 31 Jul 2026 | FEAT-002 | 31 Jul — unique title/description + OG/Twitter/canonical tags per page, Organization JSON-LD on homepage, `robots.txt`/`sitemap.xml`/`llms.txt`/`favicon.svg` added. `@astrojs/sitemap` package throws on this Astro 4.16 setup — reverted to a hand-written `sitemap.xml`, removed the dependency; revisit if upstream fixes it (manual file needs a human edit whenever a route changes). |
| FEAT-090 | Events page | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-002, event calendar | Jul 29 port; HC-072 review-only. **Aug 3 Nav:** Save my free spot / RSVP / waitlist no longer route to `/give`; interim `#ev-signup` until `seams.rsvp`. Join Homeroom/Unlock → `/give`. Email-capture UI forms shipped (Flodesk later). |
| FEAT-091 | Homeroom gated member hub (`/homeroom`) | FEAT | 2 | Nice | 📅 Scheduled | Somesh | Engineering | Jun 2026 | — | FEAT-060, FEAT-092 | — |
| FEAT-092 | Homeroom access middleware (edge + password) | FEAT | 2 | Nice | 📅 Scheduled | Somesh | Engineering | Jun 2026 | — | Edge function hosting | — |
| FEAT-093 | About Us page (v1 single page) | FEAT | 1 | Must | 🟡 In Progress | Somesh | Engineering | Jun 2026 | — | FEAT-002 | Jul 29 — ported to `src/pages/about.astro` from Dave about-deploy-rev4 (D-05). 31 Jul RF-006 QA. **Aug 3 —** hero `about-hero-team-bali-july2025.jpg`; Dan caption; Talk-with-Us email; share mailto hardened. Roster Dave sign-off still owed. |
| FEAT-094 | Campaign page template (`/festival/2026`) | FEAT | 2 | Nice | 📅 Scheduled | Somesh | Engineering | Jun 2026 | — | FEAT-080 | — |
| FEAT-100 | Pre-launch QA (a11y, Lighthouse ≥85, Keela live test) | QA | 1 | Must | 🟡 In Progress | Somesh | Somesh | Jun 2026 | — | All must-have FEATs + QA-001 | **2 Aug — audit complete, fixes not started.** Full report: [`PRE-LAUNCH-QA-AUDIT.md`](./PRE-LAUNCH-QA-AUDIT.md) (axe-core 4.12.1 + Lighthouse 13.4.1, 8 routes × desktop/mobile, real runs — no estimated scores). **0/8 pages currently pass**, but the causes cluster into a handful of shared fixes: **1 Critical, 4 Serious, 6 Moderate, 3 Minor.** Biggest wins: no `<main>` landmark + no skip link anywhere (2 lines in `BaseLayout.astro` fixes all 8 pages, and drives the single largest violation count); video-lightbox focus management on `/why`, `/our-impact`, `/schools` — **`/schools` is a genuine keyboard trap (WCAG 2.1.2 Level A)**: the Escape handler exists but is bound to `window`, so it never fires once focus enters the cross-origin YouTube iframe; `/events` filter chips declare `role="tablist"` with `aria-selected` on plain buttons and no arrow-key support. Mobile Lighthouse 56–75 on all 8 — **zero TBT, so it's purely asset weight** (unsized `<img>`, render-blocking fonts up to 3.9s, base64 images). White-on-mint contrast fails AA and is **already fixed on `/events`+`/why` but not the homepage orbit or `/give`** — apply the existing remedy. Console clean: 0 JS errors, 0 failed requests, 0 asset 404s across all 16 loads; reduced-motion verified correct (RF-009). **Still owed and not automatable:** screen-reader pass (VoiceOver/NVDA), manual text-over-image contrast review (18–60 unresolved nodes/page), Google Form iframe a11y (cross-origin), and the Keela live test (blocked on HC-075). |
| FEAT-101 | Production deploy + DNS cutover to contentment.org | OPS | 1 | Must | 🔵 Open | Somesh | Somesh | Jun 2026 | — | FEAT-100 | **Aug 4 — cutover checklist includes HC-077:** remove Footer “Project docs”, stop publishing `/docs*` on production (keep `docs/` in private repo only). See TECHNICAL-ARCHITECTURE §12 step 3b + SECURITY §8. |
| OPS-001 | Add security headers to `netlify.toml` (interim env) | OPS | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy added |
| OPS-002 | Commit `vercel.json` from spec in TECHNICAL-ARCHITECTURE §9 | OPS | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | CSP, security headers, redirects, API function timeout — committed |
| OPS-003 | CI step — auto-regenerate `contentment-home.html` on push | OPS | 1 | Should | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | Implemented as `.github/workflows/sync-single-file-build.yml` (watches `site/index.html`) |
| QA-001 | Story Board a11y gaps — dialog focus trap, `aria-pressed`, live region | QA | 1 | Must | ✅ Done | Somesh | Somesh | Jul 2026 | 3 Jul 2026 | — | Dialog semantics, focus trap, focus return, aria-pressed on toggles + filter chips, live region on count — all closed |
| DOC-001 | Fix wrong doc paths in brief footers | DOC | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | TECH-BRIEF, TEAM-BRIEF, GROWTH-BRIEF — all paths corrected |
| DOC-002 | Add `llms.txt` spec to GROWTH-BRIEF + FEAT-081 | DOC | 1 | Should | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | Comprehensive `llms.txt` guidance added to GROWTH-BRIEF §6 |
| DOC-003 | Add favicon to pre-launch checklist | DOC | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | Pre-launch blocker confirmed in GROWTH-BRIEF |
| DOC-004 | Add `rel="noopener"` to SECURITY-AND-ACCESS §8 checklist | DOC | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | Added `rel="noopener noreferrer"` check + Drive view-only confirmation to §8 |
| DOC-005 | Clarify local dev instructions in TECH-BRIEF (prototype vs post-scaffold) | DOC | 1 | Should | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | Both rows now separate: `python3 -m http.server 8080` (now) vs `astro dev` (post TICKET-002) |
| DOC-006 | Update DECISION-001 — analytics stack (Plausible → GA4 + PostHog + Clarity) | DOC | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | — | — |
| DOC-007 | Update DECISION-002 — cookie banner required (GA4 Consent Mode v2) | DOC | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | DOC-006 | — |
| DOC-008 | Update GROWTH-BRIEF §1 analytics stack | DOC | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | DOC-006 | — |
| DOC-009 | Update TECH-BRIEF + TECHNICAL-ARCHITECTURE env vars (remove Plausible, add GA4/PostHog) | DOC | 1 | Must | ✅ Done | Anik Ghosh | Somesh | Jul 2026 | 3 Jul 2026 | DOC-006 | — |
| DOC-010 | Write ANIK-REVIEW-RESPONSE.md with hyperlinks + issue tracker | DOC | 1 | Must | ✅ Done | Somesh | Somesh | Jul 2026 | 3 Jul 2026 | — | — |
| DOC-012 | Sign off DECISION-002 (Osano + Consent Mode v2) + DECISION-003 (SendGrid) | DOC | 1 | Must | ✅ Done | Somesh | Somesh Bhardwaj | Jul 2026 | 14 Jul 2026 | — | Cookie + email decisions; compliance §5.1 in SECURITY-AND-ACCESS |
| DOC-013 | Sign off DECISION-004–007 (Upstash, Astro Image, hybrid observability, PostHog Cloud) | DOC | 1 | Must | ✅ Done | Somesh | Somesh Bhardwaj | Jul 2026 | 14 Jul 2026 | DOC-012 | §5.2 production path for /privacy added |
| DOC-011 | ACCESSIBILITY.md — WCAG 2.1 AA target, ARIA map, known gaps | DOC | 1 | Must | ✅ Done | Somesh | Somesh | Jun 2026 | Jun 2026 | — | — |
| — | Plausible analytics integration | — | — | — | 🚫 Cancelled | Somesh | — | Jun 2026 | 3 Jul 2026 | — | Paid tool; GA4 + PostHog cover same needs at no cost |
| — | Supabase (replaced by GCP Cloud SQL) | — | — | — | 🚫 Cancelled | Somesh | — | Jun 2026 | Jun 2026 | — | Standardised on GCP stack |

---

## Pending decisions — unblocks multiple tickets

| Decision | Options | Waiting on | Gates |
|----------|---------|------------|-------|
| Homeroom tier amounts | $5 / $25 / $100 vs $25 / $50 / $100 | Finance / Leadership | FEAT-051 copy, FEAT-060 URL wiring |
| Keela checkout URLs | General Donation Form live (non-India); per-tier Homeroom still TBD | Finance | FEAT-060, FEAT-051 |
| Legal copy — Privacy + Terms | Final text | Legal / Ops | FEAT-071 |
| `/getinvolved` routing | Gateway page vs monthly subpath (D-03) | Product / Kristina | FEAT-050 |
| School inquiry form destination | Flodesk vs Keela vs custom API | Partnerships + Eng | FEAT-041 |
| About Us scope v1 | Single page vs 5 sub-pages | Content / Kristina | FEAT-093 |

> All engineering decisions (DECISION-001–007) resolved. See [DECISIONS.md](./DECISIONS.md).

---

## External blockers — not in engineering's control

| Blocker | Waiting on | Gates |
|---------|-----------|-------|
| Live Keela checkout URLs | Finance team | FEAT-060 (partial — general form live), FEAT-051 |
| Story content (3+ stories, photos, permissions) | Comms / Programs team | FEAT-030, FEAT-031 |
| Legal copy for `/privacy` and `/terms` | Legal / Ops | FEAT-071 |
| Social media URLs (LinkedIn, Instagram, YouTube) | Comms team | FEAT-004 |

---

## Critical path — Phase 1

```
FEAT-001 → FEAT-002* → FEAT-003
                     → FEAT-004 (needs social URLs from comms)
                     → FEAT-010 [Dave] → FEAT-011
                     → FEAT-020
                     → FEAT-031 (needs FEAT-030 — blocked on comms content)
                     → FEAT-040 → FEAT-041
                     → FEAT-050 → FEAT-060 (In Progress — General Donation Form live; per-tier + India open)
                                → FEAT-051 (blocked: per-tier Keela + D-03 routing)
                     → FEAT-070
                     → FEAT-071 (needs legal copy; cookie decision ✅ DECISION-002)
                     → FEAT-080 (analytics — all decisions resolved)
                     → FEAT-081

OPS-001  ← no dependency — ship now
OPS-002  ← no dependency — ship now
OPS-003  ← no dependency — ship now
QA-001   ← must close before FEAT-100

FEAT-100 (all must-haves + QA-001) → FEAT-101 (DNS cutover)

* = external blocker or product decision (engineering decisions 001–007 resolved)
```

---

## Related documents

| Document | Purpose |
|----------|---------|
| [FEATURE-TICKETS.md](./FEATURE-TICKETS.md) | Full ticket specs — acceptance criteria, AI prompt seeds |
| [DECISIONS.md](./DECISIONS.md) | Open and resolved technical decisions |
| [TECHNICAL-ARCHITECTURE.md](./TECHNICAL-ARCHITECTURE.md) | Stack, env vars, CI/CD, DNS runbook |
| [SECURITY-AND-ACCESS.md](./SECURITY-AND-ACCESS.md) | Security checklist, pre-launch gates |
| [ACCESSIBILITY.md](./ACCESSIBILITY.md) | WCAG 2.1 AA checklist, ARIA map, known gaps |
| [GROWTH-BRIEF](../briefs/GROWTH-BRIEF.md) | Analytics, SEO, UTM taxonomy |
| [../correspondence/ANIK-REVIEW-RESPONSE.md](../correspondence/ANIK-REVIEW-RESPONSE.md) | Final response to Anik Ghosh's engineering review — issue tracker, hyperlinked doc refs |

---

## Changelog

| Date | Change |
|------|--------|
| 4 Aug 2026 | **Page-record audit — shipped pages the team could not see, plus a wrong status.** Raised by Somesh: the team can't suggest anything about `/updates` because nobody outside engineering knew it existed. The `pages[]` "In Kristina list" column makes the gap explicit — **four routes are live or built with `No`**: `/updates` (Astro built), `/404`, `/story-board`, `/foundation-reach-map`. Worse, **`/404` was recorded "Not started" when it actually shipped 2 Aug** — corrected to "Astro built". Every one of those four rows now opens with a plain-language **PURPOSE** statement so a non-engineer reading the Sheet can tell what the page is for, and carries an explicit **NEEDS TEAM REVIEW** flag where copy/framing never had non-engineering review (`/updates`, `/404`). The two prototype routes are now noted as **publicly reachable on the preview** — "Paused" describes the design work, not the route, and that distinction was not written down anywhere. "In Kristina list" was deliberately left factual rather than flipped to `Yes`: it records her original page list, and rewriting it would erase the very gap this audit found. **Also this session:** `/updates` segment confirmed → `www.contentment.org` (`FLODESK_SEGMENT_UPDATES` set). **Double opt-in verified ON** in the Flodesk account (`app.flodesk.com/account/double-opt-in`), so `FLODESK_DOUBLE_OPTIN=true` is now set explicitly — leaving it unset would have shown "You're in" while a confirmation click was still pending, which misleads the subscriber; the form now says "check your inbox to confirm" and we send `optin_ip` + `optin_timestamp` as the EU/US consent trail. Remaining D-24 item is now just the `/events` top capture fold plus the 3 Upcoming-grid CTAs. |
| 4 Aug 2026 | **FEAT-070 Flodesk newsletter shipped — and the hosting blocker turned out to be the wrong blocker.** FEAT-070 sat Blocked since 3 Aug on "needs a hosting adapter before a server route can exist." That framing was too narrow: §6.2 only requires a *server-side* place to hold `FLODESK_API_KEY`, and a **host-native function** provides one without Astro knowing about it. Shipped `netlify/functions/newsletter.mjs` (preview) + `api/newsletter.js` (Vercel target) over a single shared core, `src/lib/flodesk.js`; `netlify.toml` rewrites `/api/newsletter` → the function so both hosts share one public path and the client needs no host check. **`astro.config.mjs` is unchanged — still `output: 'static'`, still no adapter**, so the 3 Aug security revert (unused SSR surface, 5 Dependabot alerts) stands and **FEAT-101 is no longer a blocker for FEAT-070**. The Flodesk contract was verified against developers.flodesk.com *before* writing code (Basic auth `base64('KEY:')`; `POST /v1/subscribers`, then `POST /v1/subscribers/{email}/segments` — the authoritative way to attach a segment, since an upsert alone won't re-segment an existing subscriber). This is the specific failure that caused the 31 Jul revert, so it was checked first. **11 capture points** wired across home, `/about`, `/why`, `/updates`, `/events`: honeypot field, inline `aria-live` success/error, per-submit source read, and one shared `<CaptureModal />` serving the 4 Events link-CTAs (copy + segment read off `data-capture-*` at open time, focus trap/Escape/scroll-lock mirroring `nav.js`). Segment IDs are **env-driven** (`SEGMENT_ENV_BY_SOURCE` → `FLODESK_SEGMENT_*`), never hardcoded — `scripts/flodesk-segments.mjs` prints names→IDs. **Assignments per Kristina's Miro board CTA suggestions:** 8 → `www.contentment.org` (home bottom CTA, `/about` Keep-me-in-the-loop, `/why` Send-me-something-meaningful, and on `/events` the Why-we-gather box, closing aside, both Join-the-waitlist recap links, recap Be-first-to-know); `/events` hero Save-my-free-spot → `Contentment Festival`. **Still pending Kristina + WoeiJing (D-24):** `/updates` form, `/events` top capture fold, and the 3 Upcoming-grid CTAs (Save my spot / Be first to know / Join the waitlist) which still only scroll to `#ev-signup`. Interim `FLODESK_SEGMENT_DEFAULT` → `www.contentment.org` so unconfirmed forms reach a mailed list instead of creating unsegmented subscribers. **D-19 → Resolved** (Flodesk custom-API path, host-native). **D-24 → partially resolved.** Also fixed: `/updates` was nesting a second `.news` section inside its own and rendering two headings — `NewsletterForm` gained a `bare` prop. **Behaviour change to flag:** `newsletter_submit` previously fired on button click (no backend existed); it now fires only on a confirmed subscribe, so the count will drop and start meaning something (TICKET-080). **Not yet done:** live end-to-end submit test, rate limiting (`UPSTASH_*` still unset — endpoint is public with only a honeypot), Vercel-side function verification at cutover. Updated `launch-plan-data.json` (meta, FEAT-070, D-19, D-24, integrations, `/updates` page row), `TRACKER.md`, `FEATURE-TICKETS.md`; regenerated `LaunchPlanSheet.gs` + canvas. |
| 4 Aug 2026 | **Get Involved URL slug → `/getinvolved`.** Renamed `src/pages/give.astro` → `getinvolved.astro`; updated `seams.join` / `seams.donate`, Nav, Footer, Events/Impact CTAs, sitemap. `/give` and `/give/*` 301 → `/getinvolved` on Netlify + Vercel (`/donate` → `/getinvolved#become`). Planning synced (FEAT-004/050/002, pages). |
| 4 Aug 2026 | **/give Join Homeroom → homepage Keela form with selected amount.** Tier picker ($25/$50/$100) deep-links to `/?amount=N&frequency=monthly#homeroom` via `seams.homeroomDonateUrl()`; `KeelaDonateForm` rewrites the embed iframe src with amount+frequency. Prefer `joinTiers` when those URLs land. Planning synced (FEAT-004/050/060, HC-075, pages); `LaunchPlanSheet.gs` + canvas regenerated. |
| 4 Aug 2026 | **Keela General Donation Form wired (FEAT-060 interim).** Master script in `BaseLayout` (`KeelaScripts.astro`); embed component `KeelaDonateForm.astro` replaces dummy donate widgets on homepage Homeroom + `/why` give band. Recorded in `seams.keela` (orgId + General Donation Form embed URL). Donate nav/footer → `/#homeroom`; `giveOneTime` → same. India region deferred; per-tier Homeroom `joinTiers` still empty. CSP (`netlify.toml` + `vercel.json`) allows `cdn.keela.co` script/style + `*.keela.co` connect/form-action. D-02/HC-030/HC-075 → Partial; FEAT-060 → In Progress. Regenerated `LaunchPlanSheet.gs` + canvas. |
| 3 Jul 2026 | Initial tracker. All FEAT tickets from FEATURE-TICKETS.md ported. OPS + DOC tickets added from Anik review. Done and cancelled rows added. |
| 3 Jul 2026 | OPS-001, OPS-002, OPS-003, QA-001, DOC-001, DOC-002, DOC-003 all marked ✅ Done — all Somesh-owned items from Anik's review resolved and committed. |
| 3 Jul 2026 | DOC-004, DOC-005 marked ✅ Done — `rel="noopener"` added to SECURITY-AND-ACCESS §8; local dev instructions clarified in TECH-BRIEF. All DOC tickets from Anik's review now closed. |
| 14 Jul 2026 | DECISION-004–007 signed off by Somesh Bhardwaj (Upstash, Astro Image, hybrid observability, PostHog Cloud). SECURITY-AND-ACCESS §5.2 added (production path for /privacy). DOC-013 ✅ Done. |
| 27 Jul 2026 | Schedule preponed one week per Kristina's updated Gantt — Dave handing off UI/UX + related tasks early. Design handoff Aug 3 → Jul 27 (lands with Somesh morning of Jul 28, timezone); dev sprint Aug 3–14 → Jul 27–Aug 7; final review Aug 14 → Aug 7; fixes/polish/approval Aug 17–21 → Aug 10–14; go-live Aug 24–28 → Aug 17–21. Updated `launch-plan-data.json`, `LaunchPlanSheet.gs`, `DEV-TIMELINE.html`, `dev-timelinev2.html`. |
| 27 Jul 2026 | Kristina's comments on the Launch Plan sheet incorporated: **D-01** resolved (Homeroom tiers → $25/$50/$100); **D-04** resolved (school inquiry form → Google Form + Slack integration, was Flodesk/Keela/custom API); **D-05** resolved (About Us → single page); **D-03** (`/give` routing) updated — Kristina proposes the Keela widget, pending Lorna's confirmation on Slack; **D-08** (legal copy) downgraded to Low — Privacy + Terms pages moved to Phase 2 and no longer block the Aug 17–21 launch; **D-13** (cookie consent) — Anik looped in for a second opinion, decision unchanged. About Us and Events confirmed for Phase 1 launch (both previously "1 or 2"/"1.5"). Pages renamed: "Stories" → "Our Impact", "Events & Experiences" → "Events", "Get Involved (Homeroom)" → "Get Involved". Flagged a naming overlap between the new "Our Impact" page (Phase 1, `/stories`) and the existing deferred "Impact" nav item (Phase 2, `/impact`) for future resolution. Updated `launch-plan-data.json`, `LaunchPlanSheet.gs`, `DEV-TIMELINE.html`, `dev-timelinev2.html`. |
| 27 Jul 2026 | Additional Kristina comment thread (later same day) incorporated: **D-07** resolved (social media URLs → included directly in Dave/Veron's UIUX designs, no separate Comms handoff — unblocks FEAT-004); **D-09** resolved (EIN/legal copy on `/give` → already included in the UIUX); **D-10** resolved (annual report format → PDF for Phase 1). **D-08** (Privacy/Terms legal copy) clarified as an explicit action item assigned to Somesh, with Kristina open to a ~Aug 24–28 fast-follow the week after launch instead of waiting for full Phase 2, if prioritized. Also corrected `FEAT-030`/`FEAT-051` ticket titles to match the exact wording already live in the Sheet's `Tickets` tab ("Our Impact Page data model + JSON", "Build /give/monthly Get Involved Page") and fixed a stray "Stories transparency section" reference in D-10 to "Our Impact". Updated `launch-plan-data.json`, `LaunchPlanSheet.gs`, `DEV-TIMELINE.html`, `dev-timelinev2.html`. |
| 27 Jul 2026 | Pre-handoff clarifications locked for Jul 28 call: Phase 1 hard go-live **Aug 21** (end of Phase 1; next phase TBD); final review meeting anytime **Aug 3–7**; Privacy/Terms remain open ticket (**Phase 1.5 or 2** TBD); alternative giving methods left open for Nav/Kristina/Lorna (**D-20**); Button/CTA inventory owned by Kristina (**FEAT-005**, critical). Added `handoffChecklist` (43 rows) + new Google Sheet tab **Handoff Checklist** (live tracker). Updated `launch-plan-data.json`, `LaunchPlanSheet.gs`, `docs/briefs/HANDOFF-CHECKLIST.md`. |
| 28 Jul 2026 | Handoff Checklist statuses aligned with Slack-confirmed decisions: scope freeze (7 pages + Phase 2 exclusions), Bhutan copy, $25/$50/$100 tiers, post-launch ownership, Privacy/Terms + alt-giving *approach*, analytics Ready. Still Open for live call asks (Button inventory, desktop/mobile lock, Keela URLs, /give routing, newsletter, assets, etc.). |
| 28 Jul 2026 | **UIUX→Dev handoff call** outcomes recorded: logo = live-site SVG no three dots (**D-21**); mobile owned by Somesh in sprint (**HC-021**); Jose video placeholder OK (**HC-040**); Priscilla quote + all copy changes → Phase 2 (**D-22**, **HC-042/043**); homepage-first Slack review (**HC-057**); Kristina CR form+Sheet + Miro button inventory + Claude/Lorna; Nav SVG immediate. **D-23** opened — call mentioned React; do not override **D-11 Astro** until Nick signs off. Notes: `docs/correspondence/HANDOFF-MEETING-2026-07-28.md`. Updated `launch-plan-data.json`, `LaunchPlanSheet.gs`, `HANDOFF-CHECKLIST.md`. |
| 29 Jul 2026 | **Dave's 7-page build handoff received** (Jul 29 AM, via Drive) — all Phase 1 page builds + assets + per-page handoff-report MDs landed in `handoff/2026-07-29-dave-pages/`. Intake review same day: About Us roster title resolved — Dave Kebo is **Chief Media Officer**, closing the one discrepancy against `about-name-manifest.txt` (**HC-035** → Confirmed). Photo usage rights cleared for About + For Schools photography (**HC-069**). **Our Impact route confirmed as `/our-impact`** (was `/stories`) — updated in the pages table, **FEAT-031**, and the shared `assets/seams.js` page-route config (**HC-073**). Found and fixed a site-wide CSS bug carried over from the homepage transplant: a doubled closing brace at the end of the mobile `.voice` (Kenya band) media query was silently breaking whatever rule followed it — fixed across all 9 remaining affected pages (Get Involved already had Dave's fix). Filled in the internal page-to-page `page` map in all 4 existing `assets/seams.js` copies (Home, Why Wellbeing, Get Involved, header/footer kit) so in-site nav resolves; donate/join/external-link/embed seams left empty pending Kristina's button inventory and Keela/Homeroom decisions, per standing instruction not to wire ahead of that. New open items logged: Keela Donate button color mismatch, dashboard-only fix (**HC-070**); Keela/Homeroom join-flow choreography still undecided, blocks Get Involved + Events gated CTAs (**HC-071**, broader than **D-03**); Events page missing a second round of Dave's team notes — current build is review-only, not production-final (**HC-072**); Annual Report PDFs 2019–2024 still pending from Somesh (**HC-074**). Also noted: Our Impact, For Schools, and Events builds don't yet include their own `assets/seams.js` + script include (only Home, Why Wellbeing, and Get Involved do) — needs a decision on whether to propagate before real integration. Updated `launch-plan-data.json`, `HANDOFF-CHECKLIST.md`; `LaunchPlanSheet.gs`'s embedded fallback JSON still needs a resync pass (not required for the Sheet's normal GitHub-refresh path). |
| 29 Jul 2026 | **Framework decision closed: Astro 4.x, no React (D-23 / HC-055).** Somesh signed off directly in place of the pending Nick confirmation, delegating the technical call to Engineering. Justified by an audit of Dave's actual 7-page handoff: zero external JS libraries, zero npm/build tooling, zero framework fingerprints anywhere — every page is plain HTML/CSS + vanilla inline JS (IIFEs, IntersectionObserver, scroll listeners, no `import`/`require`), matching the pattern already used in `site/index.html`. Nothing in the handoff needs client-side state or SPA routing, so there's no technical case for React. This reaffirms, rather than reopens, Anik Ghosh's original 5 Jul Astro confirmation (FEAT-002) — the Jul 28 call's mention of React is the thing being closed out, not the earlier engineering decision. Unblocks FEAT-001/FEAT-002 scaffold work to start. |
| 29 Jul 2026 | **Review-preview nav wiring activated across all 7 handoff pages** (explicitly interim — superseded once Astro's file-based routing lands under FEAT-002/TICKET-002; internal nav won't need a runtime resolver post-migration). Home, Why Wellbeing (de-inlined), Get Involved, and Our Impact each load `assets/seams.js` via a `<script src="assets/seams.js">` include (Our Impact's copy is new; Home's stray root-level copy was moved into its actual `assets/` folder, which is what the page's relative path expects). About, For Schools, Events (all 3 identical copies), and Why Wellbeing's self-contained variant have no `assets/` folder by design, so the same CONFIG + resolver was inlined directly as a `<script>` block instead of referenced externally, preserving their single-file portability. All 7 pages now cross-link to each other (about/why-wellbeing/our-impact/for-schools/events/get-involved all resolve); donate, join, external social links, and embeds remain intentionally empty pending Kristina's button inventory and the Keela/Homeroom decisions (**HC-070/071**). `events-build/head.html` is a `<head>`-only build fragment (assembled by `assemble_ev.py`), not an openable page, so it was left without its own SEAMS block. |
| 29 Jul 2026 | **Astro scaffold started (FEAT-001/002/010).** Project set up at repo root per TECHNICAL-ARCHITECTURE.md §3: `package.json`, `astro.config.mjs`, `tsconfig.json`; `node_modules/`, `dist/`, `.astro/` gitignored. Design tokens + global CSS extracted verbatim from Dave's Jul 29 homepage handoff into `src/styles/tokens.css` + `global.css` (no visual changes, per the migration rule). Built `BaseLayout.astro`, `Nav.astro`, `Footer.astro`, and 10 Home-page section components (`Hero`, `WhySplit`, `StatBand`, `VoiceBand`, `InviteBand`, `OrbitSection`, `CommunityCircles`, `Pillars`, `HomeroomBlock`, `DoorCards`, `NewsletterForm`); ported the vanilla JS (nav scroll, reveal-on-scroll, count-up, parallax, orbit pond-ripple) into `src/scripts/`. Internal nav (About/Why/Our Impact/Schools/Events/Get Involved) now links to real Astro routes instead of the interim `seams.js` pattern — those routes 404 until their own FEAT tickets land, which is expected. External/business-decision links (donate, join, socials) route through a new `src/config/seams.ts`, the Astro-native successor to the handoff's `seams.js`, still empty pending HC-030/HC-071/FEAT-005. `npm run build` passes; verified via `astro preview` that the homepage, CSS, and images all serve 200. Not yet through the homepage-first Slack review (HC-057) — that's the next gate before continuing to `/why`. |
| 29 Jul 2026 | **All 7 Phase 1 Dave pages ported into Astro under `src/pages/`.** Routes live: `/` (`index.astro`), `/about`, `/why`, `/our-impact`, `/schools`, `/events`, `/give`. Tickets flipped to In Progress: FEAT-020/031/040/050/090/093 (Events still HC-072 review-only; Give is Homeroom content at `/give` pending D-03; FEAT-051/060 still Blocked on Keela). FEAT-093 corrected to Phase 1 Must single page (D-05) — not the old “5 sub-pages” Phase 2 plan. Netlify still publishes `site/index.html` until Vercel cutover. Updated `launch-plan-data.json`; canvas refresh via `scripts/refresh-launch-canvas.py`. |
| 30 Jul 2026 | **Homepage responsive audit + mobile nav drawer (FEAT-003).** Browser-tested at 320–1280px — no horizontal scroll; all three breakpoints behave per FRONTEND-SPEC §4. Documented in `docs/planning/HOMEPAGE-RESPONSIVE-AUDIT.md`. Implemented slide-in mobile drawer in shared `Nav.astro` + `nav.js` (focus trap, Escape, body scroll lock, Join Homeroom + Donate CTAs). FEAT-003 ✅ Done. Updated `launch-plan-data.json`, `LaunchPlanSheet.gs`, timeline briefs, ACCESSIBILITY.md, FEATURE-TICKETS.md. Canvas refreshed. |
| 30 Jul 2026 | **Documentation sync pass across all planning docs/briefs following the 7-page Astro migration.** `docs/planning/FEATURE-TICKETS.md`: added a dated Status note to TICKET-010/020/031/040/050/090 (each page's Astro route + what's still outstanding); **corrected TICKET-093** from stale "About section (5 pages)" to "About Us page (v1 single page)" per D-05 (resolved 27 Jul — the 5-sub-page framing was never updated after that decision). `docs/briefs/DEV-TIMELINE.html` + `dev-timelinev2.html`: flipped FEAT-001/002/004/010/020/031/040/050/090/093 status tags from Upcoming/Ready/Blocked to In Progress with routes and open blockers noted, matching TRACKER.md/launch-plan-data.json. Verified `LaunchPlanSheet.gs`'s `EMBEDDED_JSON` fallback is already byte-identical to `launch-plan-data.json` (no edit needed — a prior pass had already resynced it). Canvas re-refreshed via `scripts/refresh-launch-canvas.py` (47 tickets, 22 HC open, 7 critical open). |
| 30 Jul 2026 | **Netlify preview cut over to Astro `dist/`.** `netlify.toml` now runs `copy-docs.sh public/docs && npm run build` and publishes `dist/` (was `site/`). Preview URL https://contentmentweb2.netlify.app/ serves all 7 Phase 1 routes. Prototypes (map, Story Board, docs hub) moved under `public/`. Footer **Project docs** link restored. Team Slack review requested (HC-057 homepage + mobile). Canvas refreshed via `scripts/refresh-launch-canvas.py`. |
| 31 Jul 2026 | **Review & Feedback Sheet tab + homepage polish.** Added `reviewFeedback` (RF-xxx) to `launch-plan-data.json` and live tab **Review & Feedback** in `LaunchPlanSheet.gs` (team QA for https://contentmentweb2.netlify.app/). Seeded RF-001–003 Done (trustcue, orbit bloom, Homeroom dummy form); RF-004–007 Open (HC-057 Slack/mobile + remaining pages + Events HC-072). Embed fallback switched to `EMBEDDED_DATA` JS object via `build-sheet-script.py`. Canvas refreshed. |
| 31 Jul 2026 | **HC-075 opened — live Keela donation checkout URLs (per tier), Owner Finance/Lorna, Critical.** Splits the Finance-side deliverable (the actual checkout links) out from **D-02** (the decision to use hosted Keela links) and **FEAT-060** (the dev ticket to wire all CTAs), since neither tracked "someone needs to hand us the URLs" as its own item. Blocks FEAT-010 close-out and FEAT-051 (`/give/monthly`). Noted: the Handoff Checklist tab on the Google Sheet is a live tab — **Refresh from source** skips tabs that already have rows (`LaunchPlanSheet.gs` `buildAllTabs_`) to protect manual team edits, so newly-added HC rows (this one, and HC-057 previously) won't appear there until **Force reseed live tabs** is run, which discards any manual Status/Owner edits made on that tab in the Sheet. Updated `launch-plan-data.json`, `TRACKER.md`; `LaunchPlanSheet.gs` regenerated via `build-sheet-script.py`. |
| 31 Jul 2026 | **Review & Feedback tab given a legend panel** (`writeReviewFeedbackLegendPanel_` in `LaunchPlanSheet.gs`, cols M–N) — it only had column dropdowns and a one-line tip before, with no explanation of the RF-xxx row format or what each Status/Severity value means, unlike Handoff Checklist's existing side legend. New panel spells out how to log a row (next sequential ID, one issue per row, leave Status/Owner/Resolution to the triager) plus Status and Severity meanings, color-matched to the conditional formatting. `onEditAudit_` updated to ignore that legend's columns (13+) so it doesn't spam the Black Box. Also confirmed and documented in `scripts/google-sheets/README.md`: the Black Box is a one-way, append-only log inside the Sheet — there is no Apps Script → GitHub write-back, so any manual Sheet edit that should change project truth has to be ported into `launch-plan-data.json`/`TRACKER.md` by hand. |
| 31 Jul 2026 | **Homepage-first Slack review gate (HC-057) cleared.** Kristina, in Slack: "Now that you've finished the Homepage, what's your estimate on timeline to finish the other pages?" — read as sign-off on the homepage build, desktop and mobile both, satisfying the Jul 28 "homepage-first, then remaining pages" agreement (HC-057). Closed **RF-004** (homepage Slack review) and **RF-005** (homepage mobile pass) as Done; **RF-006** (click-through review of the other 6 pages) is now the active review item. FEAT-010 stays In Progress — not blocked on review anymore, but still open on the Homeroom donate seam pending live Keela URLs (**HC-075**). FEAT-020 (`/why`) note updated to drop the "Slack review after homepage" blocker language. Updated `launch-plan-data.json` (meta.summary, pages/Homepage row, HC-057, RF-004/005), `TRACKER.md`, `FEATURE-TICKETS.md` (TICKET-010 status). Still owed: a timeline estimate back to Kristina for the remaining pages — not yet drafted. |
| 31 Jul 2026 | **Corrections from Somesh on the above.** (1) **FEAT-010 flipped to ✅ Done** (31 Jul) — the migration itself is complete and reviewed; the Homeroom donate button's live checkout URL was never really in this ticket's scope, it's **FEAT-060**'s (Keela wiring), just referenced from FEAT-010's notes. (2) **HC-075 owner changed Finance/Lorna → Lorna + Somesh** — both are directly engaging Keela's support team to get escalated to a senior support exec, given the delay on live checkout URLs; FEAT-060 note updated to match. (3) RF-004/RF-005 resolution notes corrected: Kristina's sign-off clears the HC-057 gate to start other pages, but **other team members' review is still ongoing in parallel** — feedback received so far has already been fixed and shipped (RF-001–003); Somesh will spot-check both rows in the Sheet directly. (4) Somesh sent Kristina a holding reply in Slack: can't commit to a firm timeline for the other 6 pages spontaneously — full answer by **4 Aug 2026**, since the design handoff actually landed Jul 28 (not Jul 27) and the homepage mobile-responsive pass needs dedicated review time, which is where most of today's time went; other milestones progressing in parallel. Pointed her at `https://contentmentweb2.netlify.app/docs/dev-timeline/classic/` as the live-updating status page — `DEV-TIMELINE.html`/`dev-timelinev2.html` "As of" date and FEAT-010 tag updated to 31 Jul/Done so that page isn't stale when she checks it. Internal ballpark for the Aug 4 answer (from FEAT-020/030/031/040/050/090/093 notes): `/why` (FEAT-020), `/our-impact` index (FEAT-031), `/schools` (FEAT-040), `/give` (FEAT-050) are all already ported (29 Jul) and only need the same responsive/QA/review pass the homepage just went through — no content blockers. `/about` (FEAT-093) needs Dave's line-by-line roster sign-off; `/events` (FEAT-090) is review-only pending Dave's second notes round (**HC-072**) — both gated on Dave, not on dev bandwidth. Individual `/stories/[slug]` (FEAT-032) stays blocked on comms delivering story content/photos (**FEAT-030**) — doesn't block the Our Impact index. Updated `launch-plan-data.json` (HC-075, FEAT-010/FEAT-060 tickets, meta.summary, RF-004/005), `TRACKER.md`, briefs. |
| 31 Jul 2026 | **FEAT-005 Owner corrected `Somesh` → `Kristina → Somesh`** to match its own note and **HC-005** (same deliverable): Kristina is finalizing the Button/CTA inventory (Miro board) to hand off; Somesh wires it once received. Updated `launch-plan-data.json`. |
| 31 Jul 2026 | **Google Sheet Tickets tab gap explained + partially closed.** Root cause: `launch-plan-data.json`'s `tickets` array (the only thing that feeds the Sheet's Tickets tab) has always been a curated subset of `TRACKER.md`'s full ticket table, not a mirror — `refresh-launch-canvas.py` already tracks this as `trackerOnly` (23 rows before this fix). Most of that gap is intentional: one-off `DOC-*`/`OPS-*`/`QA-*` housekeeping items and `CXL-*` cancelled tickets that were never meant to live on a Sheet tab scoped to active engineering tickets. But four real, still-open FEAT tickets were missing outright — added to `launch-plan-data.json`: **FEAT-011** (homepage copy audit, Open/Should), **FEAT-091/092/094** (Homeroom hub, access middleware, campaign template — all Phase 2/Scheduled). Starting now: **any ticket blocked on someone outside the team gets its Status set to Blocked/Scheduled with the blocker named in its note, and we move on to the next open ticket per FEATURE-TICKETS.md's priority order** — rather than stalling the whole sprint on one dependency. Next: FEAT-081 (SEO baseline) and FEAT-080 (Analytics) picked up as the two ready-now, no-blocker Must/Should tickets; RF-006 (responsive/QA pass on the other 6 pages) assigned to Cursor. |
| 31 Jul 2026 | **RF-006 Page 1 — About QA + polish.** Click-through at locked widths (320/390/759/768/940/1280): no horizontal overflow, all `ab-*` images load, `.anim` reveals work. Fixed: missing `public/favicon.svg` (BaseLayout 404), empty social seams in `seams.ts` wired from live-site footer per D-07 (LinkedIn/Instagram/Facebook/YouTube) + Footer `target=_blank`. About Pass-it-On cards use seams. Still open on About: Flodesk newsletter (FEAT-070), Dave roster line-by-line, donate CTA (HC-075). Next in RF-006 order: `/why`. Updated `launch-plan-data.json` (meta, About page row, FEAT-004/093, RF-006), `TRACKER.md`, `FEATURE-TICKETS.md`. |
| 31 Jul 2026 | **RF-006 Page 2 — Why QA.** Locked-width pass clean (no overflow; CEO lightbox + inline teacher reels + objections accordion OK). No code fixes needed — Homeroom Keela screenshot + newsletter remain known placeholders (HC-075 / FEAT-070). Next: `/our-impact`. Updated `launch-plan-data.json` (meta, Why page row, FEAT-020, RF-006), `TRACKER.md`. |
| 31 Jul 2026 | **FEAT-081 (SEO baseline) Done, FEAT-080 (Analytics) scaffolded.** Run as two parallel Claude subagents — FEAT-081 (Haiku, mechanical/repetitive across 7 pages) directly in the working tree, FEAT-080 (default model, judgment-heavy consent/compliance logic) in an isolated git worktree to avoid both editing `BaseLayout.astro`/`astro.config.mjs` at the same time. **FEAT-081:** per-page title/description/OG/Twitter/canonical tags, homepage Organization JSON-LD, `robots.txt`/`sitemap.xml`/`llms.txt`/`favicon.svg`. `@astrojs/sitemap` was tried for an auto-generated sitemap but throws (`Cannot read properties of undefined (reading 'reduce')`) on this Astro 4.16 + custom-Vite-plugin setup — reverted to a hand-written `sitemap.xml` and removed the dependency; a real fix would need re-testing the package later. **FEAT-080:** `src/components/Analytics.astro` bundles Osano CMP → GA4 Consent Mode v2 → Microsoft Clarity → PostHog Cloud, each independently gated on its own env var (all unset today, so nothing ships — verified `npm run build` clean both with everything unset and with test values set). Sentry via `@sentry/astro`, client-side only, registered only when `SENTRY_DSN` exists. `cta_homeroom_click` (on the still-disabled Homeroom donate button — activates automatically once Keela is live) and `newsletter_submit` (fires on the existing client-side submit stub, no backend yet) wired via a shared `src/scripts/analytics.js` helper. Added the missing `PUBLIC_CLARITY_ID` row to `TECHNICAL-ARCHITECTURE.md` §6.1, a UTM-convention note + example URL (§6.1 + `GROWTH-BRIEF.md`), and `.env.example` at repo root (key names only, full Phase-1 list). **Still open:** nobody is yet assigned to source the real GA4/PostHog/Osano/Clarity/Sentry credentials — flagged here, not yet its own numbered HC item; "pageviews recording on staging" acceptance criterion stays unchecked until `PUBLIC_GA_ID` is real. Merged the worktree branch back into `main` after review. Updated `launch-plan-data.json` (FEAT-080/081 tickets, meta.summary), `TRACKER.md`, `FEATURE-TICKETS.md` (TICKET-080/081 status + acceptance checkboxes). |
| 31 Jul 2026 | **Tickets tab now covers every real FEAT/OPS/QA ticket.** Added `OPS-001/002/003` (security headers, `vercel.json`, `contentment-home.html` CI regen) and `QA-001` (Story Board a11y) to `launch-plan-data.json`'s `tickets` array — all four are already-Done historical items, added for completeness. `DOC-*`/`CXL-*` rows stay out on purpose: those are one-off doc corrections and cancelled evaluations, not engineering tickets, so they don't belong on a tab scoped to Tickets. **Important gotcha, explains "why doesn't refresh pick this up":** `LaunchPlanSheet.gs`'s `buildAllTabs_` (what **Launch Plan → Refresh from source** calls) explicitly skips Tickets/Decisions/Handoff Checklist/Review & Feedback once they have rows in the Sheet, to protect manual edits made there — so ordinary refreshes will never pull newly-added ticket rows into an already-seeded Tickets tab. Only **Force reseed live tabs** actually rewrites it, and that discards any manual Status/Owner edits made directly on that Sheet tab. If the Tickets tab in the Sheet is still missing rows after a normal refresh, that's expected — Force reseed is the only way to close the gap, so check first whether anyone's hand-edited that tab before running it. |
| 31 Jul 2026 | **RF-006 6/6 click-through done.** Remaining pages QA'd at locked widths (320/390/759/768/940/1280): **Our Impact**, **Schools**, **Give**, **Events** (review-only per RF-007/HC-072). No P0/P1 layout/overflow/image/console bugs; no code fixes on these four. Schools matrix side-scroll / fsr-bloom treated as intentional. Events RSVP/Flodesk seams remain empty by design (HC-071 / FEAT-070). **RF-006** → Done (team can still log new RF rows per issue). **RF-007** note: Events QA found no P0/P1; HC-072 still Open. Updated `launch-plan-data.json` (meta, page rows, FEAT-031/040/050/090, RF-006/007), `TRACKER.md`, `FEATURE-TICKETS.md`. |
| 31 Jul 2026 | **RF-007 soft flags + P1 nav fix.** Shared nav `.menu-btn` clipped past viewport at ≤380px — fixed in `src/styles/global.css` (`@media(max-width:380px)` compact gap/brand/Donate/menu). Soft flags logged on RF-007 for Dave: flagship title nowrap overflow at 1280, filter chip wrap, hero contrast. HC-072 still Open. |
| 31 Jul 2026 | **FEAT-011 (copy audit) Done, FEAT-041 (school form) + FEAT-070 (newsletter) scaffolded.** Run as three parallel Claude subagents in the main working tree, each given a hard file boundary to avoid collisions (FEAT-011 explicitly excluded from touching `NewsletterForm.astro`/`HomeroomBlock.astro`, which the other two owned). **FEAT-011:** Four Pillars definitions fixed to verbatim brief wording (were paraphrased) in `Pillars.astro`; Homeroom CTA fixed to "Join Homeroom, from $5/month" in `InviteBand.astro` (was "Support a teacher monthly", linking `/give` instead of `#homeroom`). Everything else already matched; flagged (not fixed, out of scope) `HomeroomBlock.astro`'s `data-group="donor"` for whoever wires that form for real. **FEAT-041:** scaffolded per D-04 — `seams.schools.discoveryFormUrl` (empty), `/schools` renders a Google Form iframe when set, an honest "coming soon, email us" fallback when not. Blocked on someone actually creating the Google Form. **FEAT-070:** built `/updates` (linked from Footer Explore, added to `sitemap.xml`) — **but caught and reverted a real problem on review**: the subagent's first pass wired a direct client-side `fetch` to `https://api.flodesk.com/v1/forms/{id}/subscribe`, which isn't how `TECHNICAL-ARCHITECTURE.md` §6.2 specs this integration (a server-side `/api/newsletter` route using the server-only `FLODESK_API_KEY`) and isn't a verified Flodesk API contract — likely would've silently failed or thrown CORS errors the moment a real form ID landed. Reverted to the honest placeholder (analytics fires, no real send) and left a comment pointing at the real path: a hosting adapter (FEAT-101) before a server route can exist. Also reverted an unrequested env-var rename (`FLODESK_FORM_ID` → `PUBLIC_FLODESK_FORM_ID`) that had drifted from the canonical `TECHNICAL-ARCHITECTURE.md` naming without updating that doc. Updated `launch-plan-data.json` (FEAT-011/041/070 tickets, Updates page row, meta.summary), `TRACKER.md`, `FEATURE-TICKETS.md` (TICKET-011/041/070 status + acceptance checkboxes). |
| 31 Jul 2026 | **HC-076 opened — live GA4/PostHog/Osano/Clarity/Sentry IDs, Owner Somesh, Critical.** Somesh is personally sourcing all five credentials for FEAT-080 (previously flagged as unassigned). Blocks only the "pageviews recording on staging" acceptance criterion — everything else on FEAT-080 is already built and no-op-safe. Updated `launch-plan-data.json` (new HC-076, FEAT-080 note), `TRACKER.md`. |
| 31 Jul 2026 | **FEAT-041 (school discovery form) Done — real Google Form landed same-day.** `seams.schools.discoveryFormUrl` wired to the live Google Form URL; iframe height on `/schools` matched to the form's actual size (4215px, was an 800px placeholder guess) — the existing `max-height:90vh` + internal iframe scrollbar already handles the difference gracefully, no other CSS change needed. Notification-on-submit is Google Forms' own native Slack integration, not custom code, so nothing else to wire. All TICKET-041 acceptance criteria now checked. Updated `launch-plan-data.json` (FEAT-041, meta.summary), `TRACKER.md`, `FEATURE-TICKETS.md`. |
| 31 Jul 2026 | **Events light polish + D-24 (email-capture destination).** HC-072 / RF-007 stay Open (review-only; soft flags for Dave). Logged `docs/planning/EVENTS-RESPONSIVE-AUDIT.md` — no production-risk overflow; **no new Events CSS**. Opened **D-24**: Events email-capture destination (Flodesk vs Keela vs other) — Somesh asking WoeiJing + Kristina; placeholder slots stay unwired (pairs FEAT-070 / D-19). Updated `launch-plan-data.json`, `TRACKER.md`; ran `build-sheet-script.py` + `refresh-launch-canvas.py`. |
| 31 Jul 2026 | **Timeline briefs resynced to TRACKER/JSON.** `DEV-TIMELINE.html` + `dev-timelinev2.html` had drifted: Pages-at-a-glance still showed drafts/`/stories`, FEAT-011/041/081 still Upcoming, FEAT-070 Upcoming (not Blocked), design-runway items still In Progress, design handoff still listed as an external blocker. Brought FEAT statuses, page rows, pulse counts, ribbon week badges, and blockers in line with current truth (RF-006 6/6, HC-057/075/076, D-24). |
| 31 Jul 2026 | **Handoff Day 0 corrected to Jul 28.** Dave→Somesh UIUX–Dev handoff actually happened **28 Jul 2026** (earlier docs said Jul 27 / “lands morning of Jul 28 timezone”). Sprint start shifted to **Jul 28–Aug 7** (Week 1 Jul 28–31); **hard go-live stays Aug 21 as scheduled** — not slipped. Updated `launch-plan-data.json`, `DEV-TIMELINE.html`, `dev-timelinev2.html`. |
| 1 Aug 2026 | **RF-008 fixed — Our Impact video lightbox playing in a tiny frame (Kristina).** Root cause: both `/our-impact` and `/why` build their video-modal `<iframe>` by injecting it via JS `innerHTML`; Astro's scoped `<style>` only applies its scope attribute to elements present in the template at build time, so the dynamically-created iframe never matched the CSS rule meant to size it (`width/height:100%`), and it rendered at the browser's ~300×150 default instead of filling the modal. `schools.astro`'s identical lightbox already avoided this by setting size inline on the iframe string — applied the same fix to `our-impact/index.astro` and `why.astro`. Verified via headless-browser click-through on both pages (iframe now fills the modal, e.g. 1100×618 on Our Impact, 1080×607 on Why). Updated `launch-plan-data.json` (meta.summary, `reviewFeedback` RF-008, FEAT-020/031 tickets, Why/Our Impact page notes), `TRACKER.md`. |
| 1 Aug 2026 | **RF-009 closed — "How Change Happens" ripple looked static on a real iPhone 15 Pro (Chrome), not a deploy bug.** Somesh saw the pinned scroll-ripple/orbit sequence render as a plain static stack on-device, while desktop Chrome and Chrome's device toolbar (resized window / mobile emulation) both showed the full animation. Root cause: `orbit.js`/`global.css` already intentionally fall back to a static, non-pinned layout under `prefers-reduced-motion:reduce` (by design, per `ACCESSIBILITY.md`) — the phone had iOS's Reduce Motion accessibility setting on, and Chrome for iOS runs on WebKit so it honors that setting same as Safari. Device toolbar / window resizing in desktop Chrome does **not** flip this media feature, which is why it looked fine there; reproduced the exact static behavior instead via DevTools → Rendering tab → "Emulate CSS media feature prefers-reduced-motion: reduce". No code fix made — behavior is correct a11y fallback. Added explanatory comments at the reduced-motion branches in `orbit.js` (near the `matchMedia` check) and `global.css` (`@media(prefers-reduced-motion:reduce)` block for `.how-orbit`) so the team doesn't re-diagnose this from scratch on the next report. Updated `launch-plan-data.json` (meta.summary, `reviewFeedback` RF-009), `TRACKER.md`. |
| 2 Aug 2026 | **Team status update posted to Slack in Nav's requested format.** Nav's feedback (Slack, 2 Aug) asked for status updates that "lead with the outcome, current state, target date, and what you need from the requester," and for fewer recurring tasks that depend only on Somesh. Update written to that structure and posted to the team channel: outcome (all 7 Phase 1 pages built, mobile-responsive, live at contentmentweb2.netlify.app) → current state (homepage review passed 31 Jul; RF-006 6/6 QA done; RF-008 + RF-009 closed; SEO + school form live; analytics built but no-op until credentials) → target dates (**Aug 3** team inputs, **Aug 7** build-sprint end + final review window, **Aug 21** hard go-live, on track) → five named asks. Deliberately written without ticket IDs — FEAT/HC references are meaningless to Nav, WJ and Lorna, and stripping them was the single biggest lever on the "easier for non-technical people" ask; IDs stay in the Sheet for anyone who wants to dig. **Five external inputs requested by Aug 3, each with a named owner:** Keela checkout links (**Lorna**, HC-075), Button/CTA inventory Miro board (**Kristina**, HC-005/FEAT-005), About Us roster sign-off + Events second notes round (**Dave**, FEAT-093/HC-072), 3+ impact stories with photos and cleared permissions (**Comms/Programs**, FEAT-030), Events email-capture destination (**WoeiJing + Kristina**, D-24). Also addressed Nav's second point (self-service): team pointed at `/docs/dev-timeline/` for status without asking Somesh, and at the Sheet's **Review & Feedback** tab for logging preview feedback; other Sheet tabs declared locked/auto-managed with access-on-request. Honest gaps stated rather than glossed: newsletter signup doesn't send yet (needs hosting adapter, FEAT-101) and all Donate/Join Homeroom buttons are built but point nowhere pending Keela. Synced `launch-plan-data.json` (meta.summary; externalBlockers dates for Keela URLs Aug 14 → **Aug 3** and Button/CTA inventory "end of week Jul 28" → **Aug 3**, both stale; Events second-notes and a new About-roster blocker row given Aug 3; HC-005/HC-075/D-24 notes), `TRACKER.md`; regenerated `LaunchPlanSheet.gs` + canvas. |
| 2 Aug 2026 | **Netlify adapter installed (unblocks FEAT-070) + branded 404 shipped (FEAT-002).** Per Somesh: stay on Netlify through the sprint, move to Vercel at go-live, so install the adapter now rather than waiting. `astro.config.mjs` switched `output: 'static'` → `'hybrid'` with `@astrojs/netlify`. **Version pinned to 5.5.4 deliberately** — `@astrojs/netlify` 6.x requires Astro 5 and 8.x (current latest) requires Astro 7; this project is Astro 4.16, so 5.x is the only compatible line. Because `hybrid` prerenders everything by default and no route opts out with `export const prerender = false`, the built site is functionally unchanged — verified by diffing the `dist/` HTML file list before and after: identical except three files (`/foundation-reach-map/index.html`, `/story-board/index.html`, `/story-board-feed-guide/index.html`) that Astro previously emitted as meta-refresh redirect stubs and the adapter now emits as proper 301s in `dist/_redirects`. Those routes are unaffected in practice because `netlify.toml`'s `status = 200` rewrites are processed before `_redirects`. All `/docs/*` routes verified still present. `.netlify/` was already gitignored. **What this unlocks:** the server-side `/api/newsletter` route specced in TECHNICAL-ARCHITECTURE §6.2 (needs the server-only `FLODESK_API_KEY`, so it can never be client-side) is now buildable — FEAT-070's hosting blocker is cleared, leaving only **D-19** (provider choice, Engineering-owned) and the credential itself. Somesh confirmed he has a live Flodesk API key and the Flodesk MCP server, so D-19 can now close. **404 page:** `src/pages/404.astro` — `noindex, follow` injected via BaseLayout's existing `<slot name="head" />` (no layout change needed), short brand-gradient hero rather than a full `100svh` one, and 6 destination cards. Verified in-browser at 1280 and 390 px: correct 404 status on unmatched routes, no horizontal overflow at either width, all 6 cards reveal correctly on scroll. Netlify serves `dist/404.html` automatically, so no `netlify.toml` entry was needed. FEAT-100 (pre-launch a11y + Lighthouse audit) kicked off in parallel. |
| 2 Aug 2026 | **FEAT-100 pre-launch QA audit complete → `docs/planning/PRE-LAUNCH-QA-AUDIT.md`.** Run as a background subagent in an isolated git worktree so it couldn't collide with the adapter/404 work happening in the main tree; it was scoped audit-only (report, don't patch) and touched no source files. Tooling ran for real — **axe-core 4.12.1** injected via Playwright and **Lighthouse 13.4.1** programmatically, installed to a scratchpad rather than the project so `package.json` stayed clean; 8 routes × desktop + mobile. No fabricated scores. **Verdict: 0/8 pages pass today** — but that overstates the distance to launch, because the failures cluster into a few shared-chrome causes rather than 8 separate problems. **1 Critical, 4 Serious, 6 Moderate, 3 Minor.** Spot-verified the top claims against the built output before accepting them: `<main>` count is genuinely 0 in every built page, no skip link exists, and `events.astro:106–111` does declare `role="tablist"` with `aria-selected` on plain `<button>`s. The `/schools` keyboard-trap finding is subtle and correct — `schools.astro:849` *does* register an Escape handler, but on `window`, so once focus moves into the cross-origin YouTube iframe the parent document never receives the keydown and Escape cannot fire (**WCAG 2.1.2 Level A**). Two things worth calling out: (1) the white-on-mint contrast failure is **already solved in this codebase** — `events.astro:475` and `give.astro:294` carry source comments documenting it, and `/events`+`/why` switched to ink (~8:1), but the homepage orbit and `/give` kept white; the remedy just wasn't applied consistently. (2) The lightbox defect is **exactly what ACCESSIBILITY.md §4 predicted**, but that doc scopes it to the `site/story-board.html` prototypes and says to resolve it before TICKET-100 — the new Astro pages inherited the same defect, so it is *not* covered by the accepted-gaps list. Mobile perf failures (56–75) are pure asset weight: TBT is zero everywhere, so it's unsized images (16/16 runs), render-blocking Google Fonts (up to 3.9s), and ~1.3 MB recoverable on `/about`. Console/network fully clean across all 16 loads. **Explicitly not measured** (and flagged as such rather than guessed): screen readers, text-over-image contrast (18–60 `incomplete` nodes/page needing human judgement), inside the `/schools` Google Form iframe (cross-origin, axe returned `frame-tested` — also the sole cause of that page's BP 77 via third-party cookies), and the Keela live test (blocked on HC-075). Scores are localhost single-run (±3–5), so `/` desktop at 88 and `/give` at 91 need re-measurement on real hosting before anyone claims a pass; `llms-txt` fails 16/16 but is a new Lighthouse 13 audit and is ignorable. FEAT-100 → 🟡 In Progress (audit done, fixes not started). |
| 3 Aug 2026 | **CTA wiring pass + Why reels + About polish (FEAT-004/005/020/050/093).** Wired known destinations without waiting on Kristina's full Miro inventory (**HC-005** stays Open): Sign In → `https://school.contentment.org/`; Donate nav/footer falls back to `/give` until Keela (`HC-075`); homepage hero → `/why`, InviteBand → `/give`, door cards Schools → `/schools` + Events → `/events` (Spread the movement still `#` — WJ TBD); Get Involved hero Join Homeroom → `#become`. Why teacher Shorts iframes fill 9:16 `.vframe.vert` (missing absolute-fill CSS). Homeroom donate form/note measure matched to copy (Home 46ch / Why 52ch). About: hero filename `about-hero-team-bali-july2025.jpg` (byte-identical to prior asset; hi-res still pending Dave), caption "Dan in Bhutan, 2014", Talk-with-Us shows `hello@contentment.org`, share mailto hardened. Priscillah Homeroom card confirmed as Dave designed graphic (no text-free asset — not a bug). Updated `launch-plan-data.json`, `TRACKER.md`, `FEATURE-TICKETS.md`; regenerated `LaunchPlanSheet.gs` + canvas. |
| 3 Aug 2026 | **External-blockers sync fix (CTA remaining inputs).** Overview blocker for Button/CTA inventory was stale ("CTAs stay href=#"). Updated to reflect Aug 3 known-CTA wiring + what is still waiting: Kristina Miro inventory (HC-005), WJ Spread-the-movement door, WJ+Kristina D-24 email-capture, Lorna Keela HC-075/071. Added dedicated external-blocker row for Spread the movement. Note: Sheet **Refresh from source** skips live tabs (Tickets/Decisions/HC/Review) — use **Force reseed live tabs** to pull note updates into those tabs. Regenerated `LaunchPlanSheet.gs` + canvas. |
| 3 Aug 2026 | **School Google Form embed → Phase 2 (Kristina Miro).** Quote: "Let's put this on hold for phase 2. Let's test the simple page first and then integrate it if we see that people are not using the form." Embed + placeholder section commented out in `schools.astro`; `seams.schools.discoveryFormUrl` cleared (URL kept in comment). Launch CTA remains `#start` mailto / Start a Conversation (+ partner deck). **FEAT-041** → 📅 Scheduled Phase 2; **D-04** note updated (form destination still Google Form+Slack, embed deferred). Regenerated `LaunchPlanSheet.gs` + canvas. |
| 3 Aug 2026 | **Schools + Events CTA/form polish + planning sync.** Schools: partner deck Drive URL on hero+close; pricing `.col-name` → 1.4rem. Events: three email-capture slots → real first-name+email forms (Flodesk later); Join Homeroom CTAs → `/give` (`seams.join='/give'`); See the stories → `/our-impact` same tab. Sitewide content/nav text links open leaving destinations in a new tab. Updated JSON/TRACKER/FEATURE-TICKETS; regenerated `.gs` + canvas. |
| 3 Aug 2026 | **/give Homeroom tiers selectable.** Dreamer/Catalyst/Visionary are radio options; `seams.joinTiers` ready for per-tier Keela URLs. Planning synced. |
| 3 Aug 2026 | **Nav priorities — conversion paths + Homeroom offer.** (1) Events festival/RSVP/waitlist CTAs no longer send people to the donation page; interim `#ev-signup` until `seams.rsvp`/Keela. Join Homeroom stays `/give`. (2) D-01: homepage InviteBand + meta `$5` → `$25/month`; Give tiers already `$25/$50/$100`. HC-041 note updated. Benefits Events-vs-Give still may need editorial unify. Regenerated Sheet + canvas. |
| 3 Aug 2026 | **PostHog wizard PR #1 triaged down to its usable half (FEAT-080).** The PostHog GitHub app opened `posthog/instrumentation-c1ccdb` with an 11-file integration; review found the instrumentation good and the plumbing wrong, so the plumbing was reverted on the branch rather than merged. **Kept:** six new conversion events wired through the existing `src/scripts/analytics.js` `trackEvent` helper — `homeroom_join_interest` + `event_rsvp_interest` (`events.astro`), `video_started` (`src/scripts/events.js`), `school_partner_inquiry_started` + `partner_deck_downloaded` (`schools.astro`, via new `data-*` hooks rather than brittle selectors), `content_shared` (`about.astro`, both the Web Share and mailto branches). No PII in any payload, and no `identify()` — correct, since identity lives on school.contentment.org, not here. Plus the `vercel.json` CSP additions (`https://*.posthog.com` on `script-src`/`connect-src`, `worker-src blob:`). **Reverted:** (1) the PR deleted the cookieless PostHog block from `Analytics.astro` and replaced it with `src/components/posthog.astro` running PostHog defaults — localStorage+cookie persistence and `autocapture: true` — which breaks **DECISION-002/DECISION-007**'s explicit `persistence:'memory'`, `autocapture:false` requirement ("no PostHog cookies, no banner") and would have fired unconditionally while GA4 beside it still defaults to `denied` under Osano Consent Mode v2. (2) `PUBLIC_POSTHOG_KEY` → `PUBLIC_POSTHOG_PROJECT_TOKEN` in `.env.example` only, leaving `TECHNICAL-ARCHITECTURE.md` §6.1, `DECISIONS.md`, `TECH-BRIEF.md`, `launch-plan-data.json` and the real `.env` all still on the old name — same unrequested-rename failure mode as the FEAT-070 `FLODESK_FORM_ID` incident on 31 Jul. (3) `posthog-js` ^1.410.4 added to `package.json` but never imported anywhere (the component used an inline snippet) — dead dependency, removed. (4) `capture_exceptions: true`, which duplicates `@sentry/astro`, the designated error tracker per DECISIONS.md. **Also worth knowing:** the wizard's setup report claimed it instrumented `newsletter_submit` and `cta_homeroom_click` — both already existed on `main` since 31 Jul and neither file was in the diff, so the real count was 6 new events, not 8; and it claimed "the real values are configured in `.env`", which was false here (`PUBLIC_POSTHOG_KEY`/`HOST` are both empty), meaning a straight merge would have shipped zero events. Build verified clean; all six events confirmed present in `dist/`, with no PostHog init emitted (correct — key unset). Event contract is now 8 events total, all still no-op until **HC-076** credentials land. **Not fixed, flagged:** `netlify.toml` — today's actual production host — has no CSP header at all, so the `vercel.json` work is inert until the TICKET-002 cutover; and that `vercel.json` CSP is stale independently of PostHog (no `frame-src` for YouTube, which `/events` and three other pages embed; no `cmp.osano.com` on `script-src`; no Clarity on `connect-src`; still allows `plausible.io`, which DECISION-001 didn't choose). Left as a separate security pass rather than widened here. |
| 3 Aug 2026 | **CSP pass across both hosts (OPS-001/OPS-002).** Follow-on from the PostHog triage above, which surfaced that the two host configs disagreed: `vercel.json` carried a CSP, `netlify.toml` carried none — so the header existed only on the host we aren't using yet, and today's actual production surface (contentmentweb2.netlify.app) shipped with no CSP at all. Both files now carry a **byte-identical** policy, built from an inventory of every external origin in the built `dist/` rather than from the old doc spec. **Fixed a latent go-live breakage:** the old `vercel.json` `frame-src` listed only Flodesk/Keela/Raisely — no YouTube — so at the Vercel cutover every video embed on `/events`, `/why`, `/our-impact` and `/schools` would have rendered as an empty box with only a console violation to explain it. Both `www.youtube.com` and `www.youtube-nocookie.com` are needed: some iframes are built inline, others injected by JS with the non-cookie host. **Also added:** `cmp.osano.com` (script + its injected stylesheet — the CMP that gates GA4 consent was itself unlisted), `*.clarity.ms` on `connect-src` (script-src had Clarity but ingest was missing, so it would have loaded and then failed silently), `cdn.jsdelivr.net` (D3 + topojson-client on `/foundation-reach-map` and `/story-board` — both served by the same `/*` rule, so a CSP without it would have blanked both prototypes), GA4 regional/alt ingest (`*.google-analytics.com`, `*.analytics.google.com` — GA4 posts to `region1.` etc., not just the apex), `*.ingest.sentry.io`/`*.ingest.us.sentry.io` ahead of DECISION-006's DSN, `data:` on `font-src`, and `form-action 'self'` (verified safe — every form in `dist/` is JS-handled with no cross-origin `action`). **Dropped** `plausible.io`, which appears nowhere in the codebase and was never chosen by DECISION-001. **Deliberately not tightened:** `img-src` stays a broad `https:` — images come from ytimg, filesusr, pexels and Wix, and enumerating them buys little against an image-URL exfil risk that is already low; and `frame-ancestors` is still absent, matching the existing `netlify.toml` note that X-Frame-Options is omitted so responsive-testing tools can iframe the site. Verified by parsing the policy and testing every subresource in `dist/` (23 script/iframe/style/img loads) plus the runtime SDK ingest endpoints against it — all pass, nothing blocked. Not verified in a real browser: no browser tooling in this session, so **check the deploy preview's console for violations before the Vercel cutover**, particularly Osano and Clarity, which cannot be exercised locally while `PUBLIC_OSANO_CUSTOMER_ID` and `PUBLIC_CLARITY_ID` are unset. **Noticed while inventorying:** `PUBLIC_GA_ID` is now populated in the local `.env` and a real gtag.js tag ships in the local build — the first of HC-076's five credentials to land. Whether it is also set in Netlify's deploy environment is unconfirmed and worth checking, since a local-only value means staging still records nothing. |
| 3 Aug 2026 | **PostHog + Clarity credentials landed; broken loader stub fixed (HC-076 3/5, FEAT-080).** `PUBLIC_POSTHOG_KEY`/`HOST` (US region — `https://us.i.posthog.com`; the key 404s on EU) and `PUBLIC_CLARITY_ID` are now set locally alongside `PUBLIC_GA_ID`. Osano stays deliberately unset while the team reviews CMP vendors; `SENTRY_DSN` is the last one outstanding. **Real bug fixed in `Analytics.astro`:** the hand-rolled PostHog loader stub never set `__SV` and pushed queued calls onto the array itself instead of `posthog._i`. `array.js` replays init *only* from `_i` and bails unless `__SV` is set, so our `persistence:'memory'`, `autocapture:false` config was never applied — verified at runtime, `config.persistence` read `localStorage+cookie`. In other words the cookieless guarantee in **DECISION-002/007** was silently broken for as long as a key was set, which until today was never, so nothing leaked. Stub rewritten to PostHog's official shape; both `us.i.posthog.com/static/array.js` and the `-assets` host it rewrites to return 200, and the CSP's `https://*.posthog.com` covers both. Build verified: init emits with `persistence: 'memory'` and `autocapture: false`. **Clarity consent wired at the same time.** Clarity's block had no equivalent of GA4's Consent Mode default-denied, so with `PUBLIC_OSANO_CUSTOMER_ID` unset GA4 sat pinned in `denied` while the session-*replay* tool ran ungated — the inverse of what DECISION-002 intended. Microsoft's docs refined the picture rather than confirming the worst case: Clarity's own Consent Mode is **already on by default for EEA/UK/CH visitors and enforced for them since 31 Oct 2025**, so the highest-risk jurisdictions were never having cookies set without a signal. It is **not** on elsewhere unless the project's **Settings → Setup** cookie toggle is turned off. And denied does not stop collection — Clarity assigns one ID per page view and skips `_clck`/`_clsk`, so replays, heatmaps and funnels degrade rather than disappear. **Changed in code:** a default-denied `clarity('consentv2', {...})` now mirrors GA4's `gtag('consent','default')`, making the posture uniform for every visitor rather than European ones only (note the capitalised `ad_Storage`/`analytics_Storage` — Clarity's casing differs from GA4's). **Latent bug fixed alongside it:** the `osano-cm-consent-saved` bridge lived *inside* the `{GA_ID && ...}` block, so with GA4 unset there was no path to upgrade Clarity off denied at all; it now sits in its own `{OSANO_CUSTOMER_ID && ...}` block and updates each tool only if that tool actually loaded, so either can be switched off independently. **Still a team call, not a code one:** the Clarity project's Settings → Setup toggle is a dashboard setting no commit can reach, and whether cookieless session capture is acceptable without a banner is a legal question — the old comment claiming Clarity was "first-party session cookies only, no personal data, no banner needed" was replaced rather than left asserting that. Both GA4 and Clarity now sit denied until a CMP lands, which is the consistent posture but means neither reports usefully in the meantime. **Second consequence, unchanged:** **PostHog cross-page funnels are unmeasurable by design.** `persistence:'memory'` means a new anonymous `distinct_id` per page load, so unique-user counts inflate and any multi-page funnel — including the newsletter-to-Homeroom funnel the PostHog wizard configured on its dashboard — will read near-0% conversion. That is the accepted cost of cookieless, not a misconfiguration, but the dashboard will mislead anyone who doesn't know. Single-page event counts remain valid. |
| 3 Aug 2026 | **Reverted `output: 'hybrid'` → `'static'`; dropped the Netlify adapter (SEC-001, reverses the 2 Aug FEAT-002 change).** Triggered by a Dependabot review: 23 open alerts on the repo, 5 high, every one in the Astro toolchain and every fix semver-major (`astro` 4.16.19 → **7.1.6**, `@astrojs/netlify` 5.5.4 → **8.1.3**), so `npm audit fix` was a no-op. Triaged each advisory against the codebase rather than by severity: most are moot here — no server islands, no View Transitions, no `astro:assets`, no `remotePatterns`, no Cloudflare adapter, and middleware is `_noop-middleware.mjs` so the auth-bypass CVEs have no auth to bypass; the spread-attribute and `define:vars` XSS need attacker-controlled input and ours are build-time env vars ([Analytics.astro](../../src/components/Analytics.astro)). Dev-server-only ones (esbuild, Astro dev file read) plus two Windows-only (vite `fs.deny`, launch-editor NTLM) don't apply. **What was actually exposed:** hybrid mode deploys a real server surface — confirmed live, `contentmentweb2.netlify.app/_image?…` returned **500, not 404** — putting #16 (Host-header SSRF in prerendered error-page fetch, high), #2 (`X-Forwarded-Host` reflected unvalidated) and #21 (sharp/libvips CVEs, reachable via `/_image`) in play. Since no route has ever set `prerender = false`, all of that was surface with zero shipped benefit. **Change:** `output: 'static'`, `adapter` + its import removed, `@astrojs/netlify` dropped from `package.json`. **The `redirects` block had to go too** — without an adapter Astro stops emitting `dist/_redirects` and writes meta-refresh *files* hardcoded to absolute `site` URLs; a real file beats a non-forced `netlify.toml` rewrite, so `/foundation-reach-map` on the preview would have bounced visitors to production contentment.org (same trap as the existing `docs/*` note in `astro.config.mjs`). All three prototype routes were already `status = 200` rewrites in `netlify.toml`, so nothing was lost. **Verified:** `dist/` is 165 files vs hybrid's 166 — the only delta is `_redirects`; no redirect-stub directories, no meta-refresh anywhere, three prototype `.html` files intact, `.netlify/` no longer generated. Alert count 23 → **19** (the two `@astrojs/netlify` advisories retired outright; esbuild stays, it also arrives via Vite). **Cost:** FEAT-070's `/api/newsletter` is un-buildable again — Astro 4's `static` can't do per-route `prerender = false`. Astro 5 deletes `hybrid` and folds that capability into `static`, so the migration returns it for the price of re-adding an adapter. **Migration scoped, not started:** 3 majors + adapter 5→8, but a small blast radius — no content collections, no markdown, no i18n, so the v5 Content Layer rewrite and v7 Sätteri pipeline change are both no-ops across 9 pages / 14 components. Real risks are v6's script/style render-order flip (now source order), v7's Rust compiler making unclosed tags a hard error, and v7's `compressHTML` default `true` → `'jsx'` stripping inline whitespace React-style (neutralize with an explicit `compressHTML: true`) — the last two need the locked-width QA pass. **Two things to pin before starting:** `netlify.toml` sets `NODE_VERSION = "20"` but Astro 7 needs `node >=22.12.0`, so CI breaks while local (Node 23) passes; and `@astrojs/vercel@11` peers on `astro@^7` exactly like `@astrojs/netlify@8`, so **the migration and the FEAT-101 Vercel cutover are one piece of work** — splitting them pays the adapter swap twice. `@sentry/astro@10.69.0` already declares `astro >=7.0.0-beta`, not a blocker. **Also on the repo:** Dependabot *alerts* are on but **automated security fixes are off** (`automated-security-fixes` → `enabled:false`), which is why 23 alerts sat with zero PRs — worth enabling even though it can't auto-open major bumps. |
| 4 Aug 2026 | **HC-077 / FEAT-101 — production cutover must unpublish internal project docs.** At contentment.org go-live: remove Footer “Project docs” (`/docs`); stop shipping `public/docs` / skip `copy-docs.sh` on the production build; remove or 404 `/docs*` host routes. Keep `docs/` in the private GitHub repo for the team — not publicly accessible. Logged in SECURITY §8, TECHNICAL-ARCHITECTURE §12 step 3b, FEATURE-TICKETS TICKET-101, Handoff Checklist **HC-077**. Netlify preview may keep `/docs` until cutover. |
| 4 Aug 2026 | **Somesh — Get Involved donate fold + nav current page + Schools photo.** (1) `/getinvolved` new `#donate` split below Join Homeroom / above Prefer to give differently — photo left (`public/assets/gi-donate-photo.jpg`) + Keela General Donation Form on blue (same embed as homepage Homeroom; no extra white card chrome); one-time gift CTA → `#donate`. (2) Shared Nav current-page indicator: `aria-current="page"` + underline (white on transparent header, `--btnblue` on scrolled white bar; drawer too). (3) `/schools` wellbeing-lead image replaced in place at `public/assets/fs-Jadielsm.jpg`. Planning synced (FEAT-001/003/040/050/060, pages, HC-077 notes); `LaunchPlanSheet.gs` + canvas regenerated. Attribution: **Somesh Bhardwaj** (`Dev-Somesh` / somesh@contentment.org). |
| 4 Aug 2026 | **HC-076 credentials wired; CMP swapped Osano → Cookiebot (FEAT-080).** Four of five credentials now set locally — `PUBLIC_GA_ID`, `PUBLIC_POSTHOG_KEY`/`HOST` (US region), `PUBLIC_CLARITY_ID`, `PUBLIC_COOKIEBOT_ID`; `SENTRY_DSN` still outstanding. **Verified live in a real browser:** GA4 (`POST /g/collect` → **204**, `en=page_view`) and Clarity (`clarity.ms/tag/<id>` 200, `scripts.clarity.ms` 200, `POST r.clarity.ms/collect` → **204**). **PostHog — not verified by us:** it initialises correctly (`array.js` + `config.js` 200, `__loaded: true`, `persistence: 'memory'`) but no capture request was ever observable from the automated harness; proven not to be our code, since PostHog's own official snippet with its own defaults was equally silent on a minimal page. Somesh confirms events are arriving in the PostHog dashboard, so it is recorded as working **on his observation, not ours**. **Real bug fixed:** the hand-rolled PostHog stub never set `__SV` and queued `init` on the array rather than `posthog._i`, so `array.js` ignored our config and fell back to **default localStorage+cookie persistence** — the DECISION-002/007 cookieless guarantee was cosmetic for as long as a key was set (which until now was never, so nothing leaked). Rewritten to PostHog's official stub shape; runtime now reads `memory`. **CMP vendor changed** to Cookiebot with `data-blockingmode="manual"`, deliberately not `"auto"` — auto rewrites recognised tracker `<script>` tags to `type="text/plain"` until consent, which risks blocking our own inline `gtag('consent','default')` (the thing DECISION-002 depends on) and would block PostHog, cookieless under DECISION-007. `PUBLIC_OSANO_CUSTOMER_ID` → `PUBLIC_COOKIEBOT_ID` renamed across code, `.env`, `.env.example`, TECHNICAL-ARCHITECTURE §6.1, DECISIONS.md and this JSON **in one pass**, deliberately avoiding the partial-rename pattern that broke `FLODESK_FORM_ID` (31 Jul) and `PUBLIC_POSTHOG_KEY` (3 Aug). **CSP would have blocked the new CMP:** both `netlify.toml` and `vercel.json` allowlisted `cmp.osano.com` only — replaced with `consent.cookiebot.com` (script + style) and `consentcdn.cookiebot.com` (script, connect, **frame**; the banner renders in an iframe, which Osano never required). Both files kept byte-identical. **Verified live after deploy `a17e94d`:** on the registered domain the Cookiebot banner renders (`CookiebotOnDialogInit` + `CookiebotOnDialogDisplay`) and the bridge works end to end — accept fires `CookiebotOnConsentReady`/`OnAccept`, pushes `gtag('consent','update')` → granted, and GA4's next hits carry **`gcs=G111`** (was `G100`) at HTTP 204, with Cookiebot logging consent server-side. The earlier local failure was purely the unregistered-domain case. **Launch blocker recorded:** Cookiebot's free tier allows **one domain**, now registered as `contentmentweb2.netlify.app`. At the contentment.org cutover that registration must move to `www.contentment.org` or the plan be upgraded — sequence it with TICKET-002/FEAT-101 so there is no window where neither domain is registered and the banner silently no-ops. **Actions on Somesh:** register `contentmentweb2.netlify.app` + `www.contentment.org` as Cookiebot domains (else the banner no-ops in production exactly as it does locally); set the five `PUBLIC_*` vars in Netlify and redeploy with cache cleared (env vars bind at build time); keep Cookiebot's own Google Consent Mode **disabled** so it doesn't race ours; supply `SENTRY_DSN`. |
| 4 Aug 2026 | **SENTRY_DSN landed and verified — HC-076 credentials complete 5/5 (FEAT-080).** An uncaught browser error produced **4× `POST o116520.ingest.us.sentry.io/api/<project>/envelope/` → 200**, so error reporting is confirmed on the wire, not just configured. **Gotcha documented in TECHNICAL-ARCHITECTURE §6:** `SENTRY_DSN` is read via `process.env` in `astro.config.mjs`, and **`.env` does not populate `process.env`** — a local `.env` value silently ships *no* Sentry code. The `PUBLIC_*` vars are unaffected, since they resolve through `import.meta.env`, which does load `.env`. Netlify/Vercel inject build env vars so deploys are unaffected; this only bites local testing (`SENTRY_DSN=... npm run build`). Sentry's setup wizard was **not** followed: `npx astro add @sentry/astro` would duplicate the already-wired integration, and its `authToken`/`org`/`project` block fails builds without a token — same wizard-vs-repo conflict as the PostHog PR on 3 Aug. Our config stays better suited to a static site: gated on `SENTRY_DSN`, sourcemap upload disabled, client-side only. **Remaining:** paste `SENTRY_DSN` into Netlify env vars and redeploy — until then staging has no error tracking. A test issue titled "HC-076 Sentry verification error" was created in the Sentry project and should be resolved. |
| 4 Aug 2026 | **RF-010 Dave Aug 3 notes — hero zoom + Schools photo + Meet Jose hold.** Logged Dave's Website Notes (Aug 3) as **RF-010** on Review & Feedback. (1) Home / Why / Schools hero zoom was too active (`scale(1.32)` / 10s) — matched Get Involved model in shared `global.css` `.hero-bg`: `scale(1.06)` / 18s ease-out (desktop + mobile). (2) Schools Wellbeing Leads middle photo already replaced at `fs-Jadielsm.jpg` — no further action. (3) Get Involved Meet Jose / Homeroom video cut **HELD** pending WoeiJing + Kristina (Phase 2 video call) — section kept. Updated `launch-plan-data.json` (RF-010, page/ticket notes, meta.summary), `TRACKER.md`; regenerated `LaunchPlanSheet.gs` + canvas. Attribution: **Somesh Bhardwaj**. |
| 4 Aug 2026 | **Sheet usability pass + note format (HC-076 follow-up).** Three real defects in `LaunchPlanSheet.gs`, all found by diffing the dropdown lists against the values actually in `launch-plan-data.json`. **(1) The Handoff Checklist Status dropdown rejected live data** — it allowed only `[Open, Confirmed, Ready, Done]` with `setAllowInvalid(false)`, while the JSON already held **`In Progress`** (HC-061/062/063, HC-076) and **`Partial`** (HC-030, HC-075), so a Force reseed flagged or failed on those cells. **(2) Tickets `Open` and `Scheduled`, and Decisions `Open` and `Partial`, had no colour rule at all** — the old scheme was two buckets (red for Blocked/Paused, green for anything closed-ish), leaving 2 of 6 ticket statuses and 2 of 3 decision statuses rendering plain. Replaced with a single shared `STATUS_COLORS` palette giving every status its own colour, plus dropdowns on Tickets and Decisions which previously had none. **(3) Every dropdown switched to `allowInvalid: true`** (warn, don't block) — the hard-fail is what turned a stale list into a broken refresh, and the same trap was still latent on Priority and on all four Review & Feedback lists. Also added `Updates` and `404` to the Review page list: both pages shipped, but feedback on them could only be filed as 'Other'. **Note format:** notes were unreadable walls of prose — HC-076 was **3,532 chars, 13× the next longest row** (271), and FEAT-080 1,724. Both were mine. Rewritten into a `PLANNED: / DONE: / REMAINING:` bullet structure (HC-076 −52%, FEAT-080 −27%) so a reader can see intent, progress and what is still owed without parsing a paragraph. **Status:** `SENTRY_DSN` confirmed added to Netlify by Somesh. **Cookiebot deferred** — config still under active development and expected to change, so today's verified behaviour is explicitly not final and needs a full re-check before launch. |
| 4 Aug 2026 | **Ticket-status audit against acceptance criteria — 2 closed, 4 real gaps found (FEAT-001/002/020/031/040/080).** Prompted by the observation that several "In Progress" tickets looked finished. Checking `FEATURE-TICKETS.md` first showed a deeper problem than stale statuses: acceptance checkboxes had **never been ticked at all** — 0/5 on TICKET-001, 0/5 on TICKET-002, 0/4 on TICKET-020, 0/3 on TICKET-031, 0/4 on TICKET-040 — for work that shipped and passed RF-006 QA days earlier. So three layers had drifted independently: acceptance criteria, JSON statuses, and the timeline HTMLs, with the Sheet and canvas inheriting layer 2 automatically. **The audit deliberately verified each criterion against the built `dist/` rather than bulk-flipping statuses, and that caught four wrong closures.** **Closed (verified):** FEAT-001 — all 5 criteria confirmed (tokens shared, nav/footer on all 7 routes, Google Fonts link intact, `prefers-reduced-motion` in 12 built files). FEAT-080 — 7/7; the last criterion, "pageviews recording on staging", was verified live: contentmentweb2.netlify.app serves the real `G-0GBRRW2MCL` gtag tag, Cookiebot `uc.js`, and the CSP header, with all 5 HC-076 credentials landed. **NOT closed — gaps found that everyone believed were done:** (1) **FEAT-002** — `aria-current=page` is missing on `/` and `/updates`, present on the other five routes, so "active nav state" genuinely fails. (2) **FEAT-020** — `/why` has no share affordance whatsoever; the criterion requires Web Share or a copy-link fallback, and only `/about` ever got one. (3) **FEAT-031** — `/our-impact` renders 0 story cards because FEAT-030 is still blocked on comms photos/permissions; its criteria also still describe the superseded `/stories` scope. (4) **FEAT-040** — none of the required proof points (renewal rates, Harvard, five-step model) appear on `/schools`; the tier overview is present and "program rollout" language is correctly absent, so this is a content gap needing Kristina/messaging, not code. **Also caught:** FEAT-070 reads "4 Aug — SHIPPED" in its note but is 4/6 on acceptance (D-24 segment assignments + endpoint rate limiting still open) — a bulk flip would have created a fresh false record in the opposite direction. **Standing lesson:** ticket status is only trustworthy when it is checked against acceptance criteria in the build, not against the commit narrative. Timeline generation from `launch-plan-data.json` is the next step and was deliberately deferred until after this audit, since generating from unverified statuses would have rendered all four wrong closures as authoritative. |
| 5 Aug 2026 | **Two of the four audited gaps closed (FEAT-002 aria-current, FEAT-020 /why share) — and the audit's own count corrected.** Specced by three parallel subagents, then each spec put through an adversarial reviewer told to assume it was wrong and to grep every quoted string. All three came back NEEDS_CHANGES, which is the point: **the review caught a factual error in the 4 Aug audit entry above.** That entry says aria-current was "present on the other five" routes. It was present on **six** — `/our-impact` was miscounted because the check used `grep -c`, which counts matching LINES, and the minified build puts the header nav and the drawer nav on the same line. Recounted with `grep -o | wc -l`: six routes at 2 occurrences each, `/` and `/updates` at 0. The 4 Aug row is left as written (the sync rule says append, don't rewrite history); the corrected count lives in the FEAT-002 note and here. **FEAT-002 fix.** The cause was coverage, not logic — `isCurrent` in `Nav.astro` is correct and already trailing-slash-safe, but it is only ever called from the `navLinks.map` loops, and `navLinks` holds just the six header routes. `/` is linked by the brand lockup and `/updates` only from the footer, and `Footer.astro` had no current-page logic whatsoever. Fixed by marking the brand anchor in `Nav.astro` and adding the same normalization to `Footer.astro` for `/updates` only — the six nav routes are already marked in header + drawer, and a third announcement adds nothing for a screen-reader user. `/docs` stays unmarked: it is a `public/` directory index served by a netlify.toml rewrite, not an Astro route. Semantics-only — the sole `aria-current` rules in `global.css` (42, 103, 104) are scoped to `.nav-links` and `.nav-drawer__links`, so no visual change ships. Verified: `/` and `/updates` now 1 each, the six existing routes unchanged at 2, `/404` correctly 0. **FEAT-020 fix.** `/why` now carries the secondary CTA the messaging brief specifies for that page ("Secondary CTA: Share this page") — which is the stronger justification than the acceptance checkbox alone, since it was a *specified* element that was never built. Three tiers: Web Share -> clipboard copy -> mailto, so the criterion's literal "copy-link fallback" wording is met (`/about` only ever fires native_share/email). Analytics contract matches `/about` exactly — `content_shared {method, source:'why_page'}`. Placed AFTER `.homeroom-cta` so "Cancel anytime" stays adjacent to the Keela form it describes, uses the global `.btn/.btn-ghost` component rather than `/about`'s page-scoped `.cbtn`, and reuses `/about`'s approved Pass-it-On line instead of inventing copy — the reviewer flagged the first draft for doing all three the other way. `shareUrl` matches the page's own canonical including the trailing slash. **FEAT-020 deliberately stays In Progress**: 3 of its 4 criteria are still unticked. **Still open from the audit:** FEAT-031 (`/our-impact` renders 0 story cards, blocked on FEAT-030 comms photos/permissions) and FEAT-040. On FEAT-040 the 4 Aug entry was too pessimistic in the other direction — it was filed as needing Kristina, but `MESSAGING-AND-COPY.md` §Audience B already specifies the proof list ("Renewal rates, Harvard research, five-step change model, leader testimonials") and the numbers are written down (Hawai'i 90% renewal, Harvard research, five-step model). `/schools` currently carries 1 of the 5. Three are copy-placement from an approved source and need no one; only leader testimonials need sourcing. |
| 5 Aug 2026 | **Timelines are generated now — `scripts/refresh-timelines.py` (OPS-004).** Closes the root cause found on 4 Aug: `DEV-TIMELINE.html` and `dev-timelinev2.html` were the only team-facing artefacts not downstream of `launch-plan-data.json`, so while the Sheet and canvas resynced on every planning edit, the timelines could only drift — and did, sitting two days stale on the very page the team had been pointed at for self-serve status. **Design: ID-keyed in-place rewrite, not template regeneration.** Both files stay hand-authored designs; the script only rewrites the 25 ID-keyed status spans, their note cells and the `As of` stamp. Phase grouping, the Gantt, decision tables, chapter prose, owners and the deliberately-abbreviated titles are all re-emitted byte-for-byte — verified by diffing card/tid/tag/note/line counts against pristine copies before and after (identical on both files). Duplicate ids are handled by `(file, id, occurrence)` keys: FEAT-041 and FEAT-100 each hold two slots under different phase headings, so status syncs to both while notes stay frozen, since the two slots deliberately say different things. `As of` is driven by `meta.version`, not the wall clock, so bumping statuses without bumping version correctly leaves it alone. **Specced by three parallel subagents, each spec then torn apart by an adversarial reviewer** told to assume it was wrong; the generator spec came back with 12 problems, 2 blocking — including one where the author had quoted a rule string that does not exist because they'd read the *stale* condensed copy of the rule from CLAUDE.md rather than the rule file. Exactly the trap that produced the wrong answer on 4 Aug. **Two real bugs the guards caught during implementation, both of which would have silently destroyed a file:** (1) `ROW_A` keeps `<span class="tag tag-`, `">` and `</span>` OUTSIDE its capture groups, so rebuilding a row by joining `m.groups()` stripped every tag span in file A — caught only after it was written, and restored from git. (2) `CARD_B` has a *nested* group, `(<div class="note">(.*?)</div>)?`, so the same join duplicated every note in file B. That second one never reached disk, because the first prompted adding **G8**, a post-condition that re-runs the slot regex against the generated output and refuses to write if the count changed. Cheap, and it caught the nested-group bug on the very next run. **Guards (all fail-closed, verified by deliberately breaking the input):** slot counts per file, a reflow tripwire requiring every id-bearing cell to sit inside a matched slot, exactly one `As of` stamp, unknown ticket id or unknown status is a hard error rather than a silent default, <10% size delta, stale-pin detection, and G8. A restructured row exits 2 with the file byte-identical — confirmed by md5. **Idempotent, proven not asserted:** second run reports 0 changes and produces identical files; `--check` exits 1 when stale, which is a cheap pre-commit gate. Notes are synced with `--notes`, now the documented default in the rule — a synced badge next to a stale note is worse than either alone, and FEAT-080 briefly rendered `✓ Done` beside "Blocked on live IDs (HC-076)" in the same row before notes were wired in. HTML in notes is escaped last, after truncation (FEAT-100's note contains a literal `<main>` that would otherwise inject a real element mid-table). **One genuine divergence is pinned rather than overwritten:** FEAT-071 shows "Moved to Phase 2"/"Deferred" while JSON says Open, because Kristina moved Privacy/Terms out of launch scope but the ticket is still open. The pin prints its reason on every run, so it cannot rot silently. **Not managed, and reported on every run rather than hidden:** 9 JSON tickets have no timeline slot (injection refused — every slot lives in a hand-authored phase container the JSON can't map to); file A's second `Where we stand today · 27 Jul 2026` stamp; and file B's `.pulse` tiles and `.ch-counts` chapter chips, which can now contradict the synced cards. The stale header-summary prose in both files and one chapter recap line were corrected by hand in this pass. Rule updated in both mirrors plus CLAUDE.md: timelines move from a checklist item a human must remember to a script step, which is the only thing that actually changed the outcome — the 4 Aug rule already *required* timeline updates and twelve consecutive commits skipped them anyway. |
| 5 Aug 2026 | **Custom Cookiebot banner live; /privacy shipped (HC-076, FEAT-071).** The stock `CybotMultilevel` dialog was **374x800 on a 390px phone — 95% of the screen — and centred**, floating over the content. Four measured CSS passes got it to 287px but each cost more `!important` against vendor markup, so we moved to Cookiebot's **custom banner**: HTML/CSS/JS in `docs/cookiebot/` (version-controlled, not dashboard-only), wired through `cfShowCookieBanner`/`cfHideCookieBanner`. Result **390x155 mobile / 900x90 desktop**, and ~90 lines of override CSS deleted as dead weight. **`/privacy` shipped as a deliberate partial** and says so on the page: it hosts the live cookie declaration and a working Cookie Preferences trigger, fixing two genuine defects — the consent banner linked to a **404**, and Cookiebot's standard copy promised you could "withdraw your consent from the Cookie Declaration on our website" when neither the declaration nor any withdrawal path existed. Legal copy (D-08, Lorna + Finance/Legal) drops into a marked section; the cookie half needs no rework. **Consent Mode signal set completed** from Cookiebot's own GCM guide but implemented in our code rather than pasting theirs, which would have added a second `gtag('consent','default')` racing ours: `functionality_storage` + `personalization_storage` (denied by default, now upgraded from Cookiebot's *preferences* category — they were previously pinned denied even after accept), `security_storage: granted`, `ads_data_redaction: true`, `url_passthrough: false`. Verified live: `gcd` moved `13p3p3p3p5l1` → `13r3r3r3r5l1` alongside the existing `gcs=G100 → G111` at 204. `ads_data_redaction` is set but **not observed on the wire** — it governs Google Ads/Floodlight, which we don't run. **Consent method is explicit**, not implied: implied consent is invalid under GDPR/PECR (*Planet49* C-673/17) and would defeat the default-denied gate the whole architecture rests on. GTM is not used and not needed. **Action on Somesh:** run a Cookiebot rescan — the declaration currently lists only `Necessary (4)`, predating GA4/Clarity going live, so it understates what the site sets. |
| 5 Aug 2026 | **Purged stale Osano references from the Sheet data.** The CMP vendor changed on 4 Aug but `launch-plan-data.json` — the only thing feeding the Google Sheet — still described Osano as current in nine places, so the Sheet was telling the team something untrue: **D-13** ("Osano Free + GA4 Consent Mode v2"), the **integrations** row literally titled *Cookie consent banner (Osano)* and still marked Ready, the **Analytics GA4 + Clarity** row ("Osano CMP + Consent Mode v2", still scheduled for the Aug 10–14 window it had already beaten), **HC-066** ("Analytics + SEO + Osano"), **HC-076**'s own title, and the **FEAT-080** ticket title. All now name Cookiebot. **History deliberately preserved** rather than erased: D-13 and OPS-002 record that Osano was superseded and what the CSP was before, since D-13 is signed off and OPS-002 documents what actually shipped on 3 Aug. **Corrected while in there:** the PostHog integrations row still said `app.posthog.com` when the project is US-regional (`us.i.posthog.com`; the key 404s on EU) — noted as a proposed doc correction, not applied to DECISION-007 unilaterally. Sentry and Cookie-consent rows moved Ready → In Progress with their live verification results, since "Ready" understated work that is shipped and confirmed. **Also:** Sentry test issue resolved by Somesh; Cookiebot rescan running, with a note that our custom banner may stop their crawler ever observing consent-gated cookies, in which case `_ga`/`_ga_*` and `_clck`/`_clsk` need adding by hand under Statistics. |
| 5 Aug 2026 | **Full reference sweep — Osano purged from every doc, not just the Sheet.** The earlier pass fixed `launch-plan-data.json`; this one swept everything downstream of it, because the vendor change had left **44 stale references** across 14 files still describing Osano as the live CMP: `TECHNICAL-ARCHITECTURE`, `FEATURE-TICKETS`, `SECURITY-AND-ACCESS` (14 alone, mostly CSP examples naming `cmp.osano.com`), all four stakeholder briefs in **both** `.md` and `.html`, and both timeline docs. **Two traps worth recording:** (1) `refresh-timelines.py` regenerates *status slots only*, not prose — so the Osano text in `DEV-TIMELINE.html` and `dev-timelinev2.html` survived a regeneration and had to be edited directly; running the script is not sufficient for a vendor rename. (2) markdown-backtick patterns silently miss the `<code>` variants in the `.html` briefs, which is how two references survived the first pass. **DECISION-007 amended** the same way DECISION-002 was — the decision (PostHog Cloud, cookieless) stands, only the hostname moved: the project is US-region (`us.i.posthog.com`; the key **404s on EU**), and `app.posthog.com` survives as an alias, which is why `Analytics.astro` keeps it as the fallback default. **`/privacy` corrected in the pages table** — it read "Not started / Phase 1.5 or 2" on the Sheet while the page was already live. **TICKET-080 status rewritten** from "scaffolded, nothing ships yet" to the verified live state. **Left deliberately untouched:** Osano mentions in TRACKER, DECISIONS, the JSON and `ANIK-REVIEW-RESPONSE.md` — those are history and supersession records, not stale claims. **Flagged, not changed:** `refresh-timelines.py` reports a held pin keeping **FEAT-071 at "Deferred"** against the JSON's "In Progress", on the grounds that Kristina moved Privacy/Terms out of Phase 1 launch scope. The page has since shipped, so scope and build status now disagree — a team call, not a code one. |
| 5 Aug 2026 | **Shipped pages given real ticket rows; FEAT-071 re-scoped to Phase 1 + 2 and closed.** **Gap closed:** `/404` and `/updates` had shipped but existed only in the Reference tab's pages table — no Tickets-tab row at all, so anyone outside engineering tracking work on the Sheet could not see them, let alone amend them. Added retrospectively as **FEAT-072** (`/404`, shipped 2 Aug) and **FEAT-073** (`/updates`, shipped 31 Jul, Flodesk-wired 4 Aug), both flagged **NEEDS TEAM REVIEW** since neither went through Kristina's page list. The pages table now cross-references its ticket, so the two tabs agree. **FEAT-071 re-scoped by Somesh to Phase 1 + 2 and marked Done.** Rationale recorded rather than implied: Phase 1 because a dedicated page declaring our cookies and analytics is a **compliance requirement in the regions we serve** — shipping analytics without one is real exposure, not a nice-to-have — and Phase 2 because the page will keep evolving through review by Kristina, Lorna and Nav. The ticket note states explicitly what Done does and does **not** mean: the cookie half is live and verified, the legal text (controller identity, lawful basis, retention, data-subject rights, transfers, complaints) is still owed under D-08 and drops into a marked section, and `/terms` is not covered and remains not started. The page itself carries an on-page notice that the full policy is in review, so no visitor is misled by the Done status. **Both `refresh-timelines.py` FEAT-071 pins removed.** They forced "Deferred" / "Moved to Phase 2" on the premise that Kristina had taken Privacy/Terms out of Phase 1 scope; that premise no longer holds, and leaving them would have made the timelines show "Deferred" for a page that is live — precisely the rot the pin guard exists to prevent. Timelines now read ✓ Done from the JSON. **Known limitation:** FEAT-072/073 land as timeline *orphans* — timeline slots are hand-authored per phase container and the JSON offers no mapping, so they appear on the Sheet and in the JSON but not on the two timeline HTMLs until someone adds slots by hand. |
| 5 Aug 2026 | **Planning-truth overhaul — pages without tickets, statuses that lied, unreadable notes, and handoff items left open long after they closed.** Four separate defects, one root cause: the Sheet had drifted from being a record of *what is true* into a record of *what someone once typed*. **(1) SIX ROUTES HAD NO TICKET ROW.** `/terms`, `/press`, `/impact`, `/story-board`, `/foundation-reach-map` and `/docs` existed in the pages table or on the live preview with nothing on the Tickets tab, so no status view showed them and nobody outside engineering could see, question or amend them — the same gap FEAT-072/073 closed for `/404` and `/updates` on 4 Aug, still open for the rest. Added as FEAT-074, 095, 096, 034, 033 and 102, plus **OPS-004** for the timeline generator, which shipped 5 Aug and was referenced in this very changelog with no row of its own. **FEAT-071 was the worst case**: titled "Privacy & cookies — *and Terms*" and marked Done when `/privacy` shipped, which read as though `/terms` had shipped too. It has not been started. Terms is now FEAT-074, Blocked on legal copy. **FEAT-033 retitled** from "Interactive global map" to name its actual route — the old title named no artefact, so nothing indicated a live URL existed. **(2) STATUSES THAT SAID "IN PROGRESS" WHEN NOBODY ON OUR SIDE WAS PROGRESSING.** Twelve tickets sat at In Progress regardless of whether the remaining work was ours or another person's, so the Sheet could not distinguish "Somesh is building this" from "this has sat on one person for a week". Rule now written down and applied throughout: **work on us = In Progress/Done/Scheduled; fully stopped on someone else = Blocked; part landed, part owed = Partial** — and a new **Waiting on** column names the person and what they owe, on every row. Nine statuses changed: FEAT-002 → Done (the 5 Aug `aria-current` fix cleared its last criterion); FEAT-031/032/090 → Blocked (Comms story photos, Dave's Events notes); FEAT-004/005/040/050/060/093 → Partial; FEAT-101 → Scheduled (nothing undecided, the date is simply future). `Partial` had to be taught to both generators — added to `STATUS_OPTIONS.tickets` in `LaunchPlanSheet.gs` and to `STATUS_A`/`STATUS_B` in `refresh-timelines.py`, where it reuses the *progress* CSS class with its own label so no new CSS is needed in either hand-authored file. **(3) NOTE FIELDS NOBODY COULD READ.** The single "Blocker / Note" column had grown into paragraph blobs mixing today's state with three weeks of history — FEAT-002's ran to ~1,400 characters. Split into **Where it stands** (current truth, bulleted, no history) and **History** (dated bullets, newest first). Tickets go 9 → 11 columns. The Handoff Checklist deliberately **keeps its 8 columns**: `LaunchPlanSheet.gs` hardcodes its legend panel at column J with Notes at H, so an extra column would have silently collided with the legend — its Waiting-on rides as the first line of Notes instead. **(4) FIFTEEN HANDOFF ITEMS CORRECTED, 22 open → 8.** HC-027 logo — the SVG landed and is in use on every route; the item was simply never flipped. HC-028 critical-only review — ran as designed, output absorbed, pages four review rounds past that snapshot. HC-053 approvers — the names have been operating as approvers all sprint (design Dave+Veron, content Kristina+WJ, legal Lorna, go/no-go Nav+WJ, engineering Somesh); the item recorded a formality, not a gap. HC-032 → Confirmed (Flodesk live since 4 Aug). HC-022/023/025/026/033/052/060/061/062/066/076 → Confirmed, all finished work still showing unfinished. HC-067 and HC-077 → Ready (prepared, date-gated). HC-068 → In Progress, HC-074 → In Progress. HC-005/024/031/064/075 → Partial with named owners. **Cookiebot's domain move to `www.contentment.org` moved from HC-076 to HC-067**, where it belongs — it is a cutover step, not a credentials step, and the free tier allows one domain so the banner silently no-ops if it is missed. **TIMELINES: 17 orphans → 0.** A "Utility pages, prototypes & ops" block was added to *both* timeline HTMLs holding every ticket that previously had no slot — including four routes reachable on the preview right now. `EXPECTED_SLOTS` bumped 25 → 42 deliberately, since that guard is what catches accidental slot loss. `refresh-timelines.py` now reads the new **Where it stands** column, and for Blocked/Partial tickets leads the timeline note with **who it is waiting on** — the single most useful thing a stakeholder-facing timeline can say. Verified idempotent: second run reports 0 changes, `--check` exits 0. **Also repaired: the JSON had been re-serialised with `ensure_ascii=True` at some point**, escaping every em dash to `—` and inflating a two-line edit into a 554-line diff. Rewritten with `ensure_ascii=False`; no script in `scripts/` produces that form, so it came from a hand edit. |
| 5 Aug 2026 | **Lorna's Website → Keela → Finance map reviewed against the build — three real disconnects, none of them documentation nits.** Lorna published a five-stream map (website-keela.netlify.app) of what every sign-up and payment must trigger in Keela and reconcile in QuickBooks, with four decisions she needs by Fri 8/7. Checked stream by stream against `src/config/seams.ts` and the built output. **Stream 1 matches** — the General Donation Form is live on the homepage Homeroom band, the `/why` give band and `/getinvolved#donate`. **Stream 2 confirms a gap we suspected rather than knew**: the general widget *cannot* capture campaign designation as built, so homeroom, event and other designated gifts would land undesignated and receipt with the wrong tax letter. Her stating it independently turns a suspicion into a confirmed defect. **DISCONNECT 1 (HC-078, DECISION-008) — the big one.** Her map assumes every sign-up lands in **Keela**, tagged, confirmed and notified. We route all email capture sitewide to **Flodesk** (D-19, live since 4 Aug, 11 capture points across 5 pages), with Keela handling payments only, and nothing moves a Flodesk subscriber into Keela. Both cannot be the record of truth for the same person without a sync, and nobody has decided which is. Written up as DECISION-008 with three costed options; supersedes D-24, which asked the same question for `/events` alone — the answer has to be sitewide or three pages behave three ways. **DISCONNECT 2 (HC-079)** — her Stream 4 shows a **November waitlist page** that does not exist in our build and is not in the Phase 1 page list. What exists is `/events` email capture: the hero CTA opens a Flodesk CaptureModal, and three Upcoming-grid CTAs merely scroll to `#ev-signup`. She notes she and Cika scoped the festival waitlist last week *but not in relation to the website* — so the plan and the site were never reconciled. Depends on HC-078; build it before that lands and it gets built twice. **DISCONNECT 3 (HC-080) — launch date.** Her timeline ends "Sun 8/17 — Launch (MVP scope)"; this plan says **Aug 21**, with Aug 10–14 as review-and-fixes. Both are being actively worked to: she is scheduling her Keela build, her end-to-end testing and her own capacity against 8/17. If 8/17 holds, our review window *becomes* launch week and the buffer disappears. Also worth checking with her: **17 Aug 2026 is a Monday**, not a Sunday — her other four date labels (Tue 8/4, Wed 8/5, Fri 8/7, the 8/13–8/15 window) are all correct, so this looks like one slipped label rather than a different week. **HER DECISION 1 IS HALF-ANSWERED ALREADY** — D-25 opened for what Homeroom membership *includes*, but the **price half is settled**: D-01 resolved the tiers to $25/$50/$100 on 27 Jul and the `$5` entry copy was removed on 3 Aug. Only benefits are missing, and they matter because they set the tax-deductible portion of the receipt. Corrected the stale "Open — $5/$25/$100 vs $25/$50/$100" row in DECISIONS.md that had been contradicting D-01 for over a week. **HER DECISION 4 CAN CLOSE IMMEDIATELY** — she asks for access to draft pages; contentmentweb2.netlify.app has been live throughout, and every route is listed in the pages table. **HC-075/HC-030 reframed, not just re-dated**: we had been asking for three hosted checkout URLs, while her Stream 3 proposes Homeroom membership as its *own widget*. Those are different integrations — three links in `seams.joinTiers` versus a second embed with its own configuration — so this was never answerable as "send the URLs", and FEAT-051 may need re-specifying once she confirms. Four rows added to external blockers with named owners and her 8/7 deadline. |
| 5 Aug 2026 | **FEAT-096 resolved and closed the day it was opened — `/our-impact` is the impact page.** The ticket existed for exactly one reason: to give the long-standing `/impact` vs `/our-impact` naming overlap an owner. Somesh confirmed the answer is **`/our-impact`**, which is live and already the route in use, so **no separate `/impact` page will be built** — the Phase 2 nav item is superseded, not deferred. **No redirect is needed**, verified rather than assumed: `/impact` has never existed as a live route, with no reference in `src/`, `public/sitemap.xml`, `netlify.toml` or `vercel.json`. Recorded as Done rather than deleting the row, so the decision stays visible on the Sheet instead of vanishing along with the question — the pages-table row is kept for the same reason, marked "Not building". With this closed there are now **zero tickets at `Open`**: everything is either moving on our side, finished, scheduled, or explicitly waiting on a named person. **Two small defects found and fixed in passing.** The timeline note for FEAT-096 shipped containing literal `**` — these JSON note fields are authored like Markdown but land in HTML verbatim, so the asterisks were visible on both timelines. `refresh-timelines.py` now **fails closed on Markdown emphasis in any note** rather than stripping it silently, on the grounds that the JSON should not carry formatting it cannot honour. Separately, `refresh-timelines.py` does not rewrite slot *titles* (by design — they are hand-authored and deliberately abbreviated), so FEAT-096's title still read "Impact main-nav page — needs naming decision" on both files after the status flipped to Done; corrected by hand in both. Worth remembering as a general trap: a status sync is not a title sync. |
| 6 Aug 2026 | **PostHog re-verified and closed properly — it was never broken; our check was.** Somesh could see live data in the PostHog dashboard while our own record still carried "PostHog NOT verified by us" from 4 Aug, so the discrepancy was chased to root cause rather than left as one person's word against an automated result. **PostHog is fine.** Verified directly: the flags endpoint returns HTTP 200 for the live key, `array.js` returns HTTP 200 at 241,994 bytes, `api_host` is the correct `us.i.posthog.com`, and `capture_pageview` is unset — which means PostHog's default of `true` applies, so `$pageview` does fire. **Root cause of the false negative: three delays compound.** `array.js` is 242 KB loaded `async` from a *different* host (`us-assets.i.posthog.com`) than the config host; `init` is queued in our loader stub and only replayed once that parses; and `request_batching` is ON by default (confirmed as `request_batching:!0` in the shipped library), so the first `$pageview` is queued and flushed on a timer rather than sent inline. GA4's `/g/collect` and Clarity's collect fire near-immediately, so any check using a short wait or `networkidle` catches those two and misses PostHog every time. Real visitors stay on the page long enough for the flush, which is why the dashboard has data. **No code change needed** — the correct verification is to wait 5 s+ or watch for the POST to `us.i.posthog.com/i/v0/e/`, and that is now written down so the next person does not re-open this. **The separate finding is the one that actually matters.** `persistence: 'memory'` means `distinct_id` lives only for the page load, so **every page view is a new anonymous user**. Unique-user counts therefore run roughly equal to pageviews and must not be quoted as "people"; sessions do not persist across navigation; and **multi-page funnels cannot stitch** — "how many people went from `/why` to `/getinvolved`" is simply not answerable as configured. That is the deliberate price of DECISION-002/007 (cookieless, therefore needing no consent gate) and **not a bug**, but it had never been written down anywhere, and it is exactly the question the team is most likely to ask after launch. Recorded as a DECISION-007 amendment: read PostHog as page-level aggregates, use GA4 for cross-page behaviour. Changing it would mean `localStorage`, which breaks the cookieless guarantee and would then require consent-gating PostHog — a team decision on the analytics/privacy trade-off, deliberately flagged rather than actioned unilaterally. |
| 6 Aug 2026 | **HC-063 closed — every accessibility defect the pre-launch audit named is fixed and verified in the build.** Four fixes, and two of them turned out to be bigger than the audit recorded. **(1) The missing `main` landmark and skip link** was the single Critical and it repeated on every route — because the fix belonged in `BaseLayout.astro`, not on the pages. Added once; verified present on all 10 Astro routes. Screen-reader users can now jump past the nav and keyboard users no longer tab the whole header on every navigation. The skip link is positioned off-screen rather than `display:none`, which would have made it unreachable by exactly the users it exists for. Checked for regressions before wrapping the slot: no `body > child` selectors, no sibling selectors off nav/header, no script walking `body.children`, and the header is `position:fixed` so the wrapper shifts nothing. **(2) The keyboard trap was on THREE pages, not one.** The audit named `/schools`; `/why` and `/our-impact` carried the identical `window`-scoped Escape handler. Fixing only the audited page would have left two-thirds of the bug in place — a good argument for grepping the pattern rather than the finding. More importantly, **the fix could not be a keydown handler at all**: YouTube is a cross-origin iframe, so the parent document receives *no* key events from inside it, and no amount of Escape handling can reach a user whose focus is in the player. It needed focus management — focus the Close button on open, and bookend the dialog with focus **sentinels**, because a sentinel's `focus` event *does* fire in the parent document even when `keydown` does not. That is the only mechanism that contains focus around a cross-origin frame. Written once as `src/scripts/lightbox-a11y.js` and wired into all three pages; verified in the build that zero window-scoped Escape handlers remain and that the shared chunk loads on exactly those three routes and nowhere else. **(3) The `/events` filters were far worse than "missing arrow keys".** The audit said `role="tablist"` with `aria-selected` on plain buttons and no keyboard support. In fact **the chips had no click handler at all** — five dead controls that looked clickable, announced as tabs, and did nothing for anyone, keyboard or mouse. The card taxonomy (`data-access` / `data-format`) was already sitting in the markup, so the behaviour was **wired rather than the promise removed**: proper `role="tab"`, roving tabindex, Arrow/Home/End, `aria-selected` maintained, and `hidden` on filtered-out cards so their links leave the tab order rather than remaining reachable but invisible. A `role="status"` region announces the resulting count. **(4) Last white-on-mint contrast failure** fixed on the `/schools` hero button — measured at **2.26:1** against the 4.5 AA threshold, corrected to `--deep2` at **5.35:1**, matching the fix already applied to `.nav-cta`. **HC-068 deliberately stays open**, because closing it would be false: Lighthouse mobile is **56-75 against a >= 85 gate**, and that is pure asset weight with zero TBT — unsized images, render-blocking fonts, base64 images. No accessibility work moves that number; it needs an image and font pass. The Nav + WJ sign-off it also gates is an external decision, not our work. **HC-056 closed** — Claude subscription purchased and submitted to Ramp. **HC-074 closed for this phase** — the 2019-2024 Annual Reports stay on their existing Google Drive links rather than being re-hosted, a deliberate scope call since re-hosting buys nothing at launch and adds files we would then have to version. **Naming normalised to "Somesh" everywhere — 267 occurrences of "Sam" replaced across 12 files.** Sam and Somesh are the same person (Sam is the Slack handle; the handoff meeting record already said "Somesh (Sam)"), but the planning layer used the two interchangeably, so the Sheet, the timelines and the briefs read as though two people were working the sprint. Word-boundary matching only, so `Samani`/`Same`/`Saml` were untouched. |
| 6 Aug 2026 | **HC-068 perf half measured rather than assumed — it is mostly ours, not Dave's.** The remaining Lighthouse gap reads like an asset problem, so the working assumption was that it waits on Dave for optimised images and fonts. Measured against the built output instead, and the assumption was mostly wrong — recording it because acting on it would have parked a launch-gating item on someone who is not the blocker. Mobile Lighthouse is **56-75 against a >= 85 gate with zero TBT**, so it is entirely payload and layout shift, and the split is: **ON US, mechanical, no dependency** — (a) **104 of 104 `<img>` tags lack `width`/`height`**, i.e. every image on the site, which is what drives the CLS Lighthouse penalises hardest and needs no new assets from anyone; (b) **2,187 KB of base64 image data** sits inline in the built CSS/HTML, inherited from the handoff, and extracting it to real files both cuts the render payload and makes it cacheable; (c) **Google Fonts still loads render-blocking** — `display=swap` is already set, but the stylesheet `<link>` itself blocks, so the fix is a loading-strategy change in `BaseLayout`, not a font change. **WAITING ON DAVE, genuinely, but not gating the start** — optimised exports: WebP, heroes at the dimensions actually used, compressed originals. That improves the ceiling; it does not block any of the three items above. **NOT OURS AT ALL** — the Nav + WoeiJing final sign-off HC-068 also gates. Status moved Open → **Partial** with Dave named, rather than Blocked, precisely so the record does not imply we are stuck. |
| 6 Aug 2026 | **Perf pass — the payload and layout-shift half of HC-068 done, and it was almost entirely ours rather than Dave's.** Worth recording the sequence, because the working assumption was that the remaining Lighthouse gap waited on Dave for optimised images and fonts, and measuring first is what avoided parking a launch-gating item on the wrong person. Mobile Lighthouse was **56–75 against a ≥85 gate with zero TBT**, so it was pure payload and CLS. Four changes, none needing a new asset from anyone. **(1) 2,187 KB of base64 removed.** Sixteen JPEGs were inlined as data URIs in `schools.astro` and `getinvolved.astro`, inherited from Dave's handoff — one of them 665 KB on its own. Extracted to real files in `public/assets`, deduped by content hash against the existing assets so nothing was written twice under a new name. `/schools` HTML went from roughly **1.5 MB to 49 KB**; total built HTML across all ten routes is now 364 KB. The images are also now cacheable and downloaded in parallel rather than blocking the document parse. **(2) Every image given intrinsic dimensions — 104 of 104 lacked them**, which was the main CLS source. The load-bearing detail: `global.css` set `max-width:100%` but **not** `height:auto`, so adding width/height attributes alone would have squashed every image whose container is narrower than its intrinsic width — a fix that would have looked correct in the audit and wrong on the page. Added `height:auto` in the same change, after checking that the rules which genuinely want a fixed height (`.brand img`, `.pface img`, `.circ img`, `.door .ph img`) are all more specific and still win. The About roster's 24 templated headshots were handled separately since they render from a data loop; all 24 source files are uniformly 600×600, so sizing them was safe. Verified 98/98 on Astro routes; the only unsized images left are in the paused Phase 2 prototypes, which are out of audit scope. **(3) Google Fonts moved off the critical path.** `display=swap` was already set, but that governs how the font behaves *once the CSS arrives* — it does nothing about the blocking round-trip to fetch it. Now loaded via the `media="print"` swap with a `<noscript>` fallback. **Checked the CSP before shipping this**: the inline `onload` needs `script-src 'unsafe-inline'`, and without it `media` would have stayed `print` and the fonts would never have loaded at all — a silent, total failure that would have looked fine in the build. Both hosts allow it. **(4) Keela made opt-in per page.** Its stylesheet and script loaded on all ten routes from `BaseLayout`, but checking the built output showed exactly **three** mount an embed (`/`, `/why`, `/getinvolved`). The other seven were paying a render-blocking third-party stylesheet for nothing. Now a `keela` prop, documented on the `Props` interface so anyone adding `<KeelaDonateForm />` to a new page knows to set it — without it the embed mounts unstyled and never initialises. Verified per route that Keela assets are present exactly where an embed exists and nowhere else. **Still open on HC-068:** optimised exports from Dave (WebP, heroes at the dimensions actually used, compression), the manual screen-reader pass, text-over-image contrast, and a Lighthouse re-run to confirm the gate now clears. |
| 6 Aug 2026 | **HC-081 raised — six of Dave's draft deployments and Lorna's Keela map are publicly indexable, and this needs doing before cutover rather than at it.** Checked every draft URL in the pages table: all seven return **HTTP 200 with no `robots` meta tag and no `X-Robots-Tag` header**, so they are fully crawlable. **The SEO risk is the reason for the timing.** Six of them are near-complete copies of contentment.org's content sitting on public URLs with no canonical pointing back to us. Google can index them alongside — or instead of — the real site, and de-indexing a duplicate after the fact is far slower than never being indexed, so deferring this to go-live gives the copies several more weeks to accumulate. **There is also a confidentiality point**: Lorna's Website→Keela→Finance map is an internal planning document that is publicly readable, naming staff and setting out tax-letter handling, QuickBooks reconciliation and the launch plan. Useful to share deliberately; not something to leave open indefinitely. **Cheapest interim if the sites are still wanted for review** is `X-Robots-Tag: noindex` via a `_headers` file, or Netlify password protection — neither costs the reviewers anything. **We cannot action this ourselves**: they sit on Dave's and Lorna's own Netlify accounts, so it has to be asked for. Added to the §12 cutover runbook as step 11 and to FEAT-101's acceptance criteria. |
| 6 Aug 2026 | **HC-081 timing decided by Somesh — takedown requested only after reviews pass, not before.** The 6 Aug entry above recommended pulling or noindexing the six Dave draft deployments and Lorna's Keela map *before* cutover, on the SEO grounds that de-indexing a duplicate afterwards is far slower than never being indexed. Somesh weighed that and decided otherwise, for a good reason worth recording rather than leaving implicit: **those sites are the review surface.** Dave's drafts are what reviewers compare the build against, and Lorna's map is the shared reference for the Keela decisions still open (HC-078, HC-079, D-25). Pulling them mid-review would cost more than the exposure they carry. **The risk is therefore accepted knowingly, not overlooked** — six near-complete copies of contentment.org content stay indexable with no canonical back to us until launch, and Lorna's map stays publicly readable while naming staff and setting out tax-letter handling and QuickBooks reconciliation. **The zero-cost middle ground stays on the table and is worth taking if the review window stretches:** `X-Robots-Tag: noindex` via a `_headers` file, or Netlify password protection, on each site. Either removes the crawler exposure while leaving every site fully usable for reviewers — it costs them nothing and requires no takedown. Runbook step 11 and FEAT-101 acceptance both re-worded to match the decision. **Sheet note:** HC-081 is a new row, and `handoffChecklist` is one of the `LIVE_TRACKER_TABS` that `buildAllTabs_` deliberately skips once seeded — so **"Refresh from source" will not add it**. New HC or ticket rows only ever appear via **Force reseed live tabs**, which is by design (it protects in-Sheet Status edits from being overwritten on every refresh) but does mean a reseed is required whenever a row is added rather than edited. |
| 6 Aug 2026 | **Row order fixed, and made self-healing so it cannot drift again.** The Handoff Checklist read `HC-001…HC-068`, then `HC-027, HC-028, HC-042, HC-043, HC-055…HC-058`, then `HC-069…HC-081` — because rows get **appended** when created rather than inserted in position. Unusable to scan on the Sheet, and it would have recurred with every new row. Checked all four ID tables first rather than assuming: **tickets, decisions and reviewFeedback were already ascending** — only the Handoff Checklist had drifted, 30 rows out of position. **The fix is in the generator, not a one-off tidy.** `build-sheet-script.py` now sorts `tickets`, `handoffChecklist`, `decisions` and `reviewFeedback` on every run and writes the normalised order back to the JSON, so a row can be added wherever is convenient and lands correctly after the next sync. **Sort is natural, not lexical** — `HC-3 → HC-20 → HC-100`, which plain string sort gets wrong (it would put `HC-100` before `HC-20`); prefixes group so `FEAT-*` precede `OPS-*` precede `QA-*`. **Two fail-closed guards**, both tested in isolation before shipping: duplicate IDs refuse to write, because a duplicate silently shadows a real row on the Sheet; and the row set is compared before and after so sorting can only ever reorder, never gain, lose or mutate a row. Verified idempotent (a second run reports nothing) and self-healing (deliberately shuffling `HC-081` to position 4, then rebuilding, restored all 64 rows to order). Rule documented in both mirrors. **Reminder that applies to this and to the 11 rows added this session:** new rows only reach the Sheet via **Force reseed live tabs** — `handoffChecklist` is a `LIVE_TRACKER_TAB` that `buildAllTabs_` skips once seeded, so *Refresh from source* updates existing content but never adds or reorders rows. |
| 7 Aug 2026 | **Raisely alternate donation path built and gated (HC-082) — Keela blocks India and Brazil.** Lorna reported that after a month-long thread Keela confirmed donation-form traffic from **India and Brazil is blocked at the platform level**, with **no restoration timeline**, and that their suggested workaround is for affected donors to **use a VPN**. Lorna recommended launching Keela-primary with **Raisely as an alternate path**; Kristina approved and asked for the line *"Trouble loading the form? Donate here instead"* beneath the widget. **Built today, gated on the URL.** `seams.donateFallback` feeds `KeelaDonateForm`, so the line appears on all three donate surfaces — homepage Homeroom, `/why` give band, `/getinvolved#donate` — the moment Lorna's URL is filled in. It renders **only** when the URL is non-empty, verified both ways in the build (0 elements while empty, 1 per widget when set), so no placeholder or dead link can ship. **Two design calls worth recording.** First, the fallback line is **always visible** rather than revealed by a load-failure check: the Keela embed is a cross-origin iframe, so we genuinely cannot see whether it rendered for the donor — a geo-block presents as a blank frame, not a catchable error. Someone staring at nothing needs the way out already on the page, not behind a detection we cannot perform. Second, clicks fire **`donate_fallback_click {provider, source}`**. That event is the *only* signal we will ever get that the primary widget is failing for real people; without it, a region-wide outage is indistinguishable from "fewer donations this month", and it turns the next Keela conversation from anecdote into a number. **Questions put to Keela, none yet answered:** which regions besides India and Brazil are affected or at risk; whether this is Keela's own WAF configuration or a Cloudflare managed ruleset; why a platform-level regional block reached us only through a 1:1 email thread rather than proactive notice to all customers; and what the notification commitment is next time. **This also reframes the India item**: `seams.ts` previously described India as "deferred", i.e. a region we had not built for. It is now a **known block** on a region we cannot serve — a materially different thing, and D-02 was updated to say so. |
| 7 Aug 2026 | **`ANALYTICS-AND-EVENTS.md` written — the analytics wiring existed but was documented nowhere.** Prompted by a simple question after `donate_fallback_click` shipped: *where do we actually see this?* The answer required reading three source files, which is a fair definition of undocumented. It is also the source material for the planned **internal website dashboard for non-technical team members**, so it is written for two audiences on purpose — §1–§4 for Kristina, Lorna, WoeiJing and Nav; §5–§8 for engineering. **All ten live events catalogued** from the code rather than from memory — `cta_homeroom_click`, `donate_fallback_click`, `newsletter_submit`, `newsletter_error`, `content_shared`, `event_rsvp_interest`, `homeroom_join_interest`, `school_partner_inquiry_started`, `partner_deck_downloaded`, `video_started` — each with its properties, where it fires and how to read it. **The section that matters most is §4, "What these numbers cannot tell you"**, because the two biggest traps are counterintuitive and both invite confident wrong answers. First: **PostHog is the more complete source, not GA4.** GA4 and Clarity are consent-gated so they only see visitors who accepted cookies; PostHog is cookieless and sees everyone. Most people assume the opposite because GA4 is the familiar name. Second: **PostHog cannot follow a person across pages** — cookieless means no memory between page loads, so every page view looks like a new anonymous person. "Unique users" there is roughly equal to page views and must never be quoted as a number of people, and multi-page funnels are simply unanswerable; that question has to go to GA4 and accept its undercount. **`donate_fallback_click` carries its own warning** in §3: it is a floor, not a total. It only counts donors who noticed the fallback link and clicked it — anyone who saw a blank Keela frame and gave up is invisible, because a cross-origin iframe cannot be inspected. Stated explicitly so nobody reports it as a complete failure count. **§8 gives the dashboard a starting spec** — six panels, each with the caveat that must be displayed beside it, and a recommendation to build against PostHog first since its counts are complete and its API simpler. The one panel flagged to watch on launch day is donation-form failures, being the only visibility we have into a donor being unable to give. Registered in the planning README index and added to both mirrors of the sync rule, so an event added without a catalogue row is now a rule violation rather than an oversight. |
| 7 Aug 2026 | **Second a11y pass from the Lighthouse re-run — and the `--ocean` contrast failure turned out to be sitewide, not an Events bug.** The audit flagged `/events` `.ev-meta` and `.ev-recap-place` as failing contrast. Measuring rather than patching those two showed the real scope: **`--ocean` `#0080B0` is 4.45:1 on white, 4.27 on `--paper` and 3.88 on `--mist`, all under the 4.5 AA floor for body text — and it is the colour of `.eyebrow`, which appears on nearly every section of every page.** Five small-text rules were affected (`.eyebrow`, `.beyebrow`, `.dlink`, `.ev-meta`, `.ev-recap-place`). Fixed with a new **`--ocean-text: #0075A1`** token rather than by changing `--ocean` itself: the new value clears 4.5 on white (5.17), `--paper` (4.96) and `--mist` (4.51) while being visually indistinguishable at eyebrow sizes, and `--ocean` is untouched for large text, icons and non-text use, so the brand colour is unchanged. **Footer heading order fixed** — the three footer column headings were `<h4>` while the last heading before the footer is `<h2>`, skipping a level. Now `<h3>`; the built sequence reads `h3, h3, h3` and the CSS selector moved with it. **Keela iframe given a title.** Their loader injects the `<iframe>` with no `title`, so a screen reader announces only "frame" with no clue it is the donation form — flagged on all three donate routes. We cannot change what their script emits, so a `MutationObserver` labels it from `data-keela-form` once it appears. Also caught that the Raisely fallback link added earlier that same day used `--ocean` at 0.92rem — the exact failure just fixed — and moved it to `--ocean-text`. **Font loading changed again, and this one is a correction to my own 6 Aug work.** The audit reported desktop homepage **CLS 0.671**, attributed to the `hero-loaded` class. That attribution does not hold: `.anim` only animates `opacity` and `transform`, both composited, neither of which causes layout shift. The more plausible contributor is the `media="print"` font swap I introduced on 6 Aug — it avoids render-blocking but is fetched at **low priority**, which widens the window where text is laid out in fallback metrics before reflowing to Newsreader/Inter, and that reflow *is* layout shift. Switched to `rel="preload" as="style"` with an onload swap: still non-blocking, but fetched at high priority so the metric-mismatch window is as short as possible. **Stated honestly: this is reasoning, not measurement** — CLS needs re-checking in a real browser run, and the 0.671 figure should not be assumed fixed. **Deliberately NOT changed: the homepage orbit.** White text on `--ob1 #54bf98` measures **2.26:1** and on `--ob2 #32c0cf` **2.19:1**, against 4.5 for the body copy and 3.0 even for the large headings — so it fails every threshold, and no small tweak reaches compliance. The two routes to fixing it both visibly alter the homepage centrepiece: darkening the tokens (`#54bf98 → #328466`, `#32c0cf → #21828d`) or applying a 33% black scrim (which would also drag `--ob3` from `#6275b4` to `#424e79`). HC-025 does say WCAG contrast wins over strict brand palette, which authorises a fix — but *which* fix is a design decision for Dave and Veron, not one to make silently in a commit. Numbers documented here so that conversation starts from measurements. |
| 7 Aug 2026 | **PostHog Toolbar was rendering as giant unstyled icons — a CSP gap, not a build leak.** Somesh spotted a column of huge black icons and the word "Inspect" below the footer on the deployed preview. First read was Astro's dev toolbar leaking into a production build, which would have been serious; checked and ruled out — no `astro-dev-toolbar`, `dev-toolbar` or `devToolbar` string appears anywhere in `dist/`, `public/`, or the live HTML fetched from the preview. It is the **PostHog Toolbar**, and the cause is precise: `script-src` already allowed `https://*.posthog.com`, so the toolbar's **JavaScript loaded**, while `style-src` did **not** include it, so its **stylesheet was blocked** — leaving the component mounted with no CSS. Fixed by adding `https://*.posthog.com` to `style-src` in **both** `netlify.toml` and `vercel.json`, kept byte-identical per OPS-001/002, with the reasoning written inline so it is not stripped later as an unexplained widening. Risk is negligible: `style-src` already carries `'unsafe-inline'`, which is far weaker than naming one additional host. **Worth stating clearly: this was never public-facing.** The PostHog Toolbar only loads for someone who has launched it from the PostHog app — it is injected client-side for that authenticated session and is never delivered to ordinary visitors, which is exactly why the deployed HTML is clean. What it did break was a genuinely useful tool: the toolbar overlays click maps and heatmaps on the real site, which is directly relevant to the internal dashboard work. Documented in `ANALYTICS-AND-EVENTS.md` alongside a second gotcha found while checking — because PostHog runs `persistence: 'memory'` for the cookieless guarantee (DECISION-007), it cannot durably store the toolbar token, so the toolbar may need re-launching on each page load. That is a deliberate trade-off rather than a fault, but it will otherwise look like one to whoever hits it next. |
| 7 Aug 2026 | **Full records sync — and it caught four things that had gone quietly stale.** Ran the whole chain rather than only the generators, on the principle that "everything is committed" is not the same as "everything is true". **(1) `meta.version` was still `2026-08-05`.** That value drives the `As of` stamp on both timeline briefs, so the pages the team is pointed at for self-serve status were claiming to be two days older than the work in them. Bumped to `2026-08-07`; both stamps regenerated. This is the failure mode `refresh-timelines.py` was built to prevent, and it still slipped — because the script faithfully renders whatever `meta.version` says, and nobody had updated it. Worth remembering: bumping statuses without bumping version leaves the stamp wrong *by design*. **(2) `meta.summary` rewritten** — it described the 5 Aug state and mentioned none of the accessibility closure, the WebP/redirect performance work, the Keela India/Brazil block, the Raisely fallback, or the analytics documentation. **(3) 17 stale `/stories` references** across GROWTH-BRIEF and TEAM-BRIEF, left over from the 27 Jul rename to `/our-impact`. Fixed with a deliberately narrow pattern — `/stories/[slug]` and `/stories/[educator-name]` are still the valid Phase 1.5 routes under FEAT-032 and were left alone. **The replacement then damaged three sentences** by rewriting the *from* side of "renamed `/stories` → `/our-impact`" into "`/our-impact` → `/our-impact`", which is nonsense; caught on review and repaired. Recorded because it is the obvious hazard of bulk find-and-replace on prose that *describes* a rename. **(4) TECH-BRIEF was asserting something untrue.** Its stack table listed image optimization as "Astro `<Image />` (`astro:assets`) — build-time WebP + srcset", stated as fact with a sign-off date. The build has never used `astro:assets`: every image is a plain `<img>` from `public/`, which Astro copies verbatim and cannot touch. Corrected to say what is actually true — hand-converted WebP, 26 MB → 11 MB, intrinsic dimensions on every tag, **and no `srcset`, so mobile still downloads desktop-sized images**. That last point matters and was being hidden by the incorrect claim. Also repaired an earlier sweep's damage in the same table: "PostHog Cloud (`us.i.posthog.com`; `us.i.posthog.com` is an alias)" was self-referential; the alias is `app.posthog.com`. |
