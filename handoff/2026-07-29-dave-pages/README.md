# Dave handoff — 2026-07-29

Raw drop folder for the 7 finalized UIUX pages + assets Dave sent via Google Drive
(per `docs/correspondence/HANDOFF-MEETING-2026-07-28.md`, "Asset zip + Handoff Report MDs — Dave → Somesh").

Drop each page folder (and its subfolders/assets) here as-is, unmodified, so we keep an
unaltered copy of what was handed off before anything gets adapted into `site/` or the
Astro migration (`TICKET-002`).

Not yet integrated into the live build — do not link to files here from `site/index.html`.
